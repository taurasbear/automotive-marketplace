import { ListingSearch } from "@/features/search";

const MainPage = () => {
  return (
    <div className="mx-auto mt-64 min-h-screen">
      <ListingSearch className="w-full" />
    </div>
  );
};

export default MainPage;

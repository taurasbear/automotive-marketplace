# Variant + i18n Infrastructure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the `Car` entity (per-listing vehicle instance) with a `Variant` entity (shared vehicle template), promote the four vehicle-attribute enums (Fuel, Transmission, BodyType, Drivetrain) to first-class DB entities with i18n translation tables, and update the full stack (domain → infra → application → tests → frontend) to reflect these changes.

**Architecture:** The domain model gains a `Variant` entity (shared spec template) that listings reference via FK, plus a new `Drivetrain` entity. Four `*Translation` tables store EN/LT display names for each lookup entity. The application's `CreateListingCommand` handler uses find-or-create logic: reuse an existing matching Variant (standard) or create a new one (custom/new).

**Tech Stack:** .NET 9, EF Core (Postgres/Npgsql), AutoMapper, MediatR, Bogus (test data), xUnit + FluentAssertions + NSubstitute, React 18, TypeScript, TanStack Query, Zod, react-hook-form, TanStack Router.

---

## File Map

### New Files
- `Automotive.Marketplace.Domain/Entities/Drivetrain.cs`
- `Automotive.Marketplace.Domain/Entities/FuelTranslation.cs`
- `Automotive.Marketplace.Domain/Entities/TransmissionTranslation.cs`
- `Automotive.Marketplace.Domain/Entities/BodyTypeTranslation.cs`
- `Automotive.Marketplace.Domain/Entities/DrivetrainTranslation.cs`
- `Automotive.Marketplace.Infrastructure/Data/Configuration/VariantConfiguration.cs`
- `Automotive.Marketplace.Infrastructure/Data/Configuration/DrivetrainConfiguration.cs`
- `Automotive.Marketplace.Infrastructure/Data/Configuration/FuelTranslationConfiguration.cs`
- `Automotive.Marketplace.Infrastructure/Data/Configuration/TransmissionTranslationConfiguration.cs`
- `Automotive.Marketplace.Infrastructure/Data/Configuration/BodyTypeTranslationConfiguration.cs`
- `Automotive.Marketplace.Infrastructure/Data/Configuration/DrivetrainTranslationConfiguration.cs`
- `Automotive.Marketplace.Infrastructure/Data/Builders/VariantBuilder.cs`
- `Automotive.Marketplace.Infrastructure/Data/Seeders/FuelSeeder.cs`
- `Automotive.Marketplace.Infrastructure/Data/Seeders/TransmissionSeeder.cs`
- `Automotive.Marketplace.Infrastructure/Data/Seeders/BodyTypeSeeder.cs`
- `Automotive.Marketplace.Infrastructure/Data/Seeders/DrivetrainSeeder.cs`
- `Automotive.Marketplace.Infrastructure/Data/Seeders/VariantSeeder.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/CreateVariant/CreateVariantCommand.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/CreateVariant/CreateVariantCommandHandler.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/DeleteVariant/DeleteVariantCommand.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/DeleteVariant/DeleteVariantCommandHandler.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/GetAllVariants/GetAllVariantsQuery.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/GetAllVariants/GetAllVariantsQueryHandler.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/GetAllVariants/GetAllVariantsResponse.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantById/GetVariantByIdQuery.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantById/GetVariantByIdQueryHandler.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantById/GetVariantByIdResponse.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/UpdateVariant/UpdateVariantCommand.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/UpdateVariant/UpdateVariantCommandHandler.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantsByModel/GetVariantsByModelQuery.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantsByModel/GetVariantsByModelQueryHandler.cs`
- `Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantsByModel/GetVariantsByModelResponse.cs`
- `Automotive.Marketplace.Application/Mappings/VariantMappings.cs`
- `Automotive.Marketplace.Server/Controllers/VariantController.cs`
- `automotive.marketplace.client/src/api/queryKeys/variantKeys.ts`
- `automotive.marketplace.client/src/features/variantList/` (full folder — see Task 15)
- `automotive.marketplace.client/src/app/pages/Variants.tsx`
- `automotive.marketplace.client/src/app/routes/variants.tsx`
- `automotive.marketplace.client/src/components/forms/select/VariantSelect.tsx`

### Modified Files
- `Automotive.Marketplace.Domain/Entities/Variant.cs`
- `Automotive.Marketplace.Domain/Entities/Listing.cs`
- `Automotive.Marketplace.Domain/Entities/Fuel.cs`
- `Automotive.Marketplace.Domain/Entities/BodyType.cs`
- `Automotive.Marketplace.Domain/Entities/Transmission.cs`
- `Automotive.Marketplace.Domain/Entities/Model.cs`
- `Automotive.Marketplace.Domain/Enums/Permission.cs`
- `Automotive.Marketplace.Infrastructure/Data/Configuration/ListingConfiguration.cs`
- `Automotive.Marketplace.Infrastructure/Data/DatabaseContext/AutomotiveContext.cs`
- `Automotive.Marketplace.Infrastructure/Data/Seeders/ListingSeeder.cs`
- `Automotive.Marketplace.Infrastructure/Data/Builders/ListingBuilder.cs`
- `Automotive.Marketplace.Infrastructure/ServiceExtensions.cs`
- `Automotive.Marketplace.Application/Features/EnumFeatures/GetBodyTypes/*` (handler + response)
- `Automotive.Marketplace.Application/Features/EnumFeatures/GetFuelTypes/*` (handler + response)
- `Automotive.Marketplace.Application/Features/EnumFeatures/GetTransmissionTypes/*` (handler + response)
- `Automotive.Marketplace.Application/Features/EnumFeatures/GetDrivetrainTypes/*` (handler + response)
- `Automotive.Marketplace.Application/Features/ListingFeatures/CreateListing/CreateListingCommand.cs`
- `Automotive.Marketplace.Application/Features/ListingFeatures/CreateListing/CreateListingCommandHandler.cs`
- `Automotive.Marketplace.Application/Features/ListingFeatures/GetAllListings/GetAllListingsResponse.cs`
- `Automotive.Marketplace.Application/Features/ListingFeatures/GetAllListings/GetAllListingsQueryHandler.cs`
- `Automotive.Marketplace.Application/Features/ListingFeatures/GetListingById/GetListingByIdResponse.cs`
- `Automotive.Marketplace.Application/Features/ListingFeatures/GetListingById/GetListingByIdQueryHandler.cs`
- `Automotive.Marketplace.Application/Features/ListingFeatures/UpdateListing/UpdateListingCommand.cs`
- `Automotive.Marketplace.Application/Features/ListingFeatures/UpdateListing/UpdateListingCommandHandler.cs`
- `Automotive.Marketplace.Application/Mappings/EnumMappings.cs`
- `Automotive.Marketplace.Application/Mappings/ListingMappings.cs`
- `Automotive.Marketplace.Tests/Features/HandlerTests/ListingHandlerTests/CreateListingCommandHandlerTests.cs`
- `Automotive.Marketplace.Tests/Features/HandlerTests/ListingHandlerTests/GetAllListingsQueryHandlerTests.cs`
- `automotive.marketplace.client/src/constants/endpoints.ts`
- `automotive.marketplace.client/src/constants/permissions.ts`
- `automotive.marketplace.client/src/types/enum/GetBodyTypesResponse.ts`
- `automotive.marketplace.client/src/types/enum/GetFuelTypesResponse.ts`
- `automotive.marketplace.client/src/types/enum/GetTransmissionTypesResponse.ts`
- `automotive.marketplace.client/src/types/enum/GetDrivetrainTypesResponse.ts`
- `automotive.marketplace.client/src/components/forms/DrivetrainToggleGroup.tsx`
- `automotive.marketplace.client/src/components/forms/TransmissionToggleGroup.tsx`
- `automotive.marketplace.client/src/components/forms/select/FuelSelect.tsx`
- `automotive.marketplace.client/src/components/forms/select/BodyTypeSelect.tsx`
- `automotive.marketplace.client/src/features/createListing/components/CreateListingForm.tsx`
- `automotive.marketplace.client/src/features/createListing/schemas/createListingSchema.ts`
- `automotive.marketplace.client/src/features/createListing/types/CreateListingCommand.ts`
- `automotive.marketplace.client/src/features/createListing/types/CreateListingFormData.ts`
- `automotive.marketplace.client/src/features/listingList/types/GetAllListingsResponse.ts`
- `automotive.marketplace.client/src/features/listingList/components/ListingCard.tsx`

### Deleted Files
- `Automotive.Marketplace.Domain/Enums/Drivetrain.cs`
- `Automotive.Marketplace.Infrastructure/Data/Configuration/CarConfiguration.cs`
- `Automotive.Marketplace.Infrastructure/Data/Seeders/CarSeeder.cs`
- `Automotive.Marketplace.Infrastructure/Data/Builders/CarBuilder.cs`
- `Automotive.Marketplace.Application/Features/CarFeatures/` (entire folder)
- `Automotive.Marketplace.Application/Mappings/CarMappings.cs`
- `Automotive.Marketplace.Server/Controllers/CarController.cs`
- `automotive.marketplace.client/src/api/queryKeys/carKeys.ts`
- `automotive.marketplace.client/src/features/carList/` (entire folder)
- `automotive.marketplace.client/src/app/pages/Cars.tsx`
- `automotive.marketplace.client/src/app/routes/cars.tsx`

---

## Task 1: Domain — Fix Variant + Add Drivetrain Entity + Translation Entities

**Files:**
- Modify: `Automotive.Marketplace.Domain/Entities/Variant.cs`
- Create: `Automotive.Marketplace.Domain/Entities/Drivetrain.cs`
- Create: `Automotive.Marketplace.Domain/Entities/FuelTranslation.cs`
- Create: `Automotive.Marketplace.Domain/Entities/TransmissionTranslation.cs`
- Create: `Automotive.Marketplace.Domain/Entities/BodyTypeTranslation.cs`
- Create: `Automotive.Marketplace.Domain/Entities/DrivetrainTranslation.cs`

- [ ] **Step 1: Replace Variant.cs content**

```csharp
// Automotive.Marketplace.Domain/Entities/Variant.cs
namespace Automotive.Marketplace.Domain.Entities;

public class Variant : BaseEntity
{
    public int Year { get; set; }

    public bool IsCustom { get; set; }

    public int DoorCount { get; set; }

    public int PowerKw { get; set; }

    public int EngineSizeMl { get; set; }

    public Guid ModelId { get; set; }

    public virtual Model Model { get; set; } = null!;

    public Guid FuelId { get; set; }

    public virtual Fuel Fuel { get; set; } = null!;

    public Guid TransmissionId { get; set; }

    public virtual Transmission Transmission { get; set; } = null!;

    public Guid BodyTypeId { get; set; }

    public virtual BodyType BodyType { get; set; } = null!;

    public virtual ICollection<Listing> Listings { get; set; } = [];
}
```

- [ ] **Step 2: Create Drivetrain.cs**

```csharp
// Automotive.Marketplace.Domain/Entities/Drivetrain.cs
namespace Automotive.Marketplace.Domain.Entities;

public class Drivetrain : BaseEntity
{
    public string Name { get; set; } = string.Empty;

    public virtual ICollection<DrivetrainTranslation> Translations { get; set; } = [];
}
```

- [ ] **Step 3: Create FuelTranslation.cs**

```csharp
// Automotive.Marketplace.Domain/Entities/FuelTranslation.cs
namespace Automotive.Marketplace.Domain.Entities;

public class FuelTranslation : BaseEntity
{
    public Guid FuelId { get; set; }

    public string LanguageCode { get; set; } = string.Empty;

    public string Name { get; set; } = string.Empty;

    public virtual Fuel Fuel { get; set; } = null!;
}
```

- [ ] **Step 4: Create TransmissionTranslation.cs**

```csharp
// Automotive.Marketplace.Domain/Entities/TransmissionTranslation.cs
namespace Automotive.Marketplace.Domain.Entities;

public class TransmissionTranslation : BaseEntity
{
    public Guid TransmissionId { get; set; }

    public string LanguageCode { get; set; } = string.Empty;

    public string Name { get; set; } = string.Empty;

    public virtual Transmission Transmission { get; set; } = null!;
}
```

- [ ] **Step 5: Create BodyTypeTranslation.cs**

```csharp
// Automotive.Marketplace.Domain/Entities/BodyTypeTranslation.cs
namespace Automotive.Marketplace.Domain.Entities;

public class BodyTypeTranslation : BaseEntity
{
    public Guid BodyTypeId { get; set; }

    public string LanguageCode { get; set; } = string.Empty;

    public string Name { get; set; } = string.Empty;

    public virtual BodyType BodyType { get; set; } = null!;
}
```

- [ ] **Step 6: Create DrivetrainTranslation.cs**

```csharp
// Automotive.Marketplace.Domain/Entities/DrivetrainTranslation.cs
namespace Automotive.Marketplace.Domain.Entities;

public class DrivetrainTranslation : BaseEntity
{
    public Guid DrivetrainId { get; set; }

    public string LanguageCode { get; set; } = string.Empty;

    public string Name { get; set; } = string.Empty;

    public virtual Drivetrain Drivetrain { get; set; } = null!;
}
```

- [ ] **Step 7: Add Translations nav property to Fuel, Transmission, BodyType**

```csharp
// Automotive.Marketplace.Domain/Entities/Fuel.cs
namespace Automotive.Marketplace.Domain.Entities;

public class Fuel : BaseEntity
{
    public string Name { get; set; } = string.Empty;

    public virtual ICollection<FuelTranslation> Translations { get; set; } = [];
}
```

```csharp
// Automotive.Marketplace.Domain/Entities/Transmission.cs
namespace Automotive.Marketplace.Domain.Entities;

public class Transmission : BaseEntity
{
    public string Name { get; set; } = string.Empty;

    public virtual ICollection<TransmissionTranslation> Translations { get; set; } = [];
}
```

```csharp
// Automotive.Marketplace.Domain/Entities/BodyType.cs
namespace Automotive.Marketplace.Domain.Entities;

public class BodyType : BaseEntity
{
    public string Name { get; set; } = string.Empty;

    public virtual ICollection<BodyTypeTranslation> Translations { get; set; } = [];
}
```

---

## Task 2: Domain — Update Listing, Model; Delete Drivetrain Enum

**Files:**
- Modify: `Automotive.Marketplace.Domain/Entities/Listing.cs`
- Modify: `Automotive.Marketplace.Domain/Entities/Model.cs`
- Delete: `Automotive.Marketplace.Domain/Enums/Drivetrain.cs`

- [ ] **Step 1: Replace Listing.cs content**

The current diff left `Drivetrain` as an enum field. Replace it with a FK + entity nav, and remove the `using Automotive.Marketplace.Domain.Enums;` import.

```csharp
// Automotive.Marketplace.Domain/Entities/Listing.cs
using Automotive.Marketplace.Domain.Enums;

namespace Automotive.Marketplace.Domain.Entities;

public class Listing : BaseEntity
{
    public decimal Price { get; set; }

    public string City { get; set; } = string.Empty;

    public Status Status { get; set; }

    public string? Description { get; set; }

    public string? Vin { get; set; }

    public string? Colour { get; set; }

    public bool IsUsed { get; set; }

    public int Mileage { get; set; }

    public bool IsSteeringWheelRight { get; set; }

    public Guid DrivetrainId { get; set; }

    public virtual Drivetrain Drivetrain { get; set; } = null!;

    public Guid VariantId { get; set; }

    public virtual Variant Variant { get; set; } = null!;

    public Guid SellerId { get; set; }

    public virtual User Seller { get; set; } = null!;

    public virtual ICollection<Image> Images { get; set; } = [];

    public virtual ICollection<User> LikeUsers { get; set; } = [];
}
```

Note: `Status` still comes from `Automotive.Marketplace.Domain.Enums` so keep the using.

- [ ] **Step 2: Update Model.cs — replace Cars collection with Variants**

```csharp
// Automotive.Marketplace.Domain/Entities/Model.cs
namespace Automotive.Marketplace.Domain.Entities;

public class Model : BaseEntity
{
    public string Name { get; set; } = string.Empty;

    public Guid MakeId { get; set; }

    public virtual Make Make { get; set; } = null!;

    public virtual ICollection<Variant> Variants { get; set; } = [];
}
```

- [ ] **Step 3: Delete Drivetrain enum**

```bash
rm Automotive.Marketplace.Domain/Enums/Drivetrain.cs
```

---

## Task 3: Domain — Update Permission Enum

**Files:**
- Modify: `Automotive.Marketplace.Domain/Enums/Permission.cs`

- [ ] **Step 1: Rename Car permissions to Variant**

```csharp
// Automotive.Marketplace.Domain/Enums/Permission.cs
namespace Automotive.Marketplace.Domain.Enums;

public enum Permission
{
    CreateListings,

    ManageListings,

    ViewListings,

    ViewModels,

    CreateModels,

    ManageModels,

    ViewVariants,

    CreateVariants,

    ManageVariants
}
```

---

## Task 4: Infrastructure — EF Configurations

**Files:**
- Delete: `Automotive.Marketplace.Infrastructure/Data/Configuration/CarConfiguration.cs`
- Create: `Automotive.Marketplace.Infrastructure/Data/Configuration/VariantConfiguration.cs`
- Create: `Automotive.Marketplace.Infrastructure/Data/Configuration/DrivetrainConfiguration.cs`
- Create: `Automotive.Marketplace.Infrastructure/Data/Configuration/FuelTranslationConfiguration.cs`
- Create: `Automotive.Marketplace.Infrastructure/Data/Configuration/TransmissionTranslationConfiguration.cs`
- Create: `Automotive.Marketplace.Infrastructure/Data/Configuration/BodyTypeTranslationConfiguration.cs`
- Create: `Automotive.Marketplace.Infrastructure/Data/Configuration/DrivetrainTranslationConfiguration.cs`
- Modify: `Automotive.Marketplace.Infrastructure/Data/Configuration/ListingConfiguration.cs`

- [ ] **Step 1: Delete CarConfiguration.cs**

```bash
rm Automotive.Marketplace.Infrastructure/Data/Configuration/CarConfiguration.cs
```

- [ ] **Step 2: Create VariantConfiguration.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/Configuration/VariantConfiguration.cs
using Automotive.Marketplace.Domain.Entities;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace Automotive.Marketplace.Infrastructure.Data.Configuration;

public class VariantConfiguration : IEntityTypeConfiguration<Variant>
{
    public void Configure(EntityTypeBuilder<Variant> builder)
    {
        builder.HasOne(v => v.Model)
            .WithMany(m => m.Variants)
            .HasForeignKey(v => v.ModelId);

        builder.HasOne(v => v.Fuel)
            .WithMany()
            .HasForeignKey(v => v.FuelId);

        builder.HasOne(v => v.Transmission)
            .WithMany()
            .HasForeignKey(v => v.TransmissionId);

        builder.HasOne(v => v.BodyType)
            .WithMany()
            .HasForeignKey(v => v.BodyTypeId);
    }
}
```

- [ ] **Step 3: Create DrivetrainConfiguration.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/Configuration/DrivetrainConfiguration.cs
using Automotive.Marketplace.Domain.Entities;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace Automotive.Marketplace.Infrastructure.Data.Configuration;

public class DrivetrainConfiguration : IEntityTypeConfiguration<Drivetrain>
{
    public void Configure(EntityTypeBuilder<Drivetrain> builder)
    {
        builder.HasMany(d => d.Translations)
            .WithOne(t => t.Drivetrain)
            .HasForeignKey(t => t.DrivetrainId);
    }
}
```

- [ ] **Step 4: Create FuelTranslationConfiguration.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/Configuration/FuelTranslationConfiguration.cs
using Automotive.Marketplace.Domain.Entities;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace Automotive.Marketplace.Infrastructure.Data.Configuration;

public class FuelTranslationConfiguration : IEntityTypeConfiguration<FuelTranslation>
{
    public void Configure(EntityTypeBuilder<FuelTranslation> builder)
    {
        builder.HasOne(t => t.Fuel)
            .WithMany(f => f.Translations)
            .HasForeignKey(t => t.FuelId);

        builder.HasIndex(t => new { t.FuelId, t.LanguageCode }).IsUnique();
    }
}
```

- [ ] **Step 5: Create TransmissionTranslationConfiguration.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/Configuration/TransmissionTranslationConfiguration.cs
using Automotive.Marketplace.Domain.Entities;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace Automotive.Marketplace.Infrastructure.Data.Configuration;

public class TransmissionTranslationConfiguration : IEntityTypeConfiguration<TransmissionTranslation>
{
    public void Configure(EntityTypeBuilder<TransmissionTranslation> builder)
    {
        builder.HasOne(t => t.Transmission)
            .WithMany(tr => tr.Translations)
            .HasForeignKey(t => t.TransmissionId);

        builder.HasIndex(t => new { t.TransmissionId, t.LanguageCode }).IsUnique();
    }
}
```

- [ ] **Step 6: Create BodyTypeTranslationConfiguration.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/Configuration/BodyTypeTranslationConfiguration.cs
using Automotive.Marketplace.Domain.Entities;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace Automotive.Marketplace.Infrastructure.Data.Configuration;

public class BodyTypeTranslationConfiguration : IEntityTypeConfiguration<BodyTypeTranslation>
{
    public void Configure(EntityTypeBuilder<BodyTypeTranslation> builder)
    {
        builder.HasOne(t => t.BodyType)
            .WithMany(b => b.Translations)
            .HasForeignKey(t => t.BodyTypeId);

        builder.HasIndex(t => new { t.BodyTypeId, t.LanguageCode }).IsUnique();
    }
}
```

- [ ] **Step 7: Create DrivetrainTranslationConfiguration.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/Configuration/DrivetrainTranslationConfiguration.cs
using Automotive.Marketplace.Domain.Entities;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace Automotive.Marketplace.Infrastructure.Data.Configuration;

public class DrivetrainTranslationConfiguration : IEntityTypeConfiguration<DrivetrainTranslation>
{
    public void Configure(EntityTypeBuilder<DrivetrainTranslation> builder)
    {
        builder.HasOne(t => t.Drivetrain)
            .WithMany(d => d.Translations)
            .HasForeignKey(t => t.DrivetrainId);

        builder.HasIndex(t => new { t.DrivetrainId, t.LanguageCode }).IsUnique();
    }
}
```

- [ ] **Step 8: Replace ListingConfiguration.cs**

Replace the existing FK to Car and remove the Drivetrain enum conversion. Add DrivetrainId FK. Status enum conversion stays.

```csharp
// Automotive.Marketplace.Infrastructure/Data/Configuration/ListingConfiguration.cs
using Automotive.Marketplace.Domain.Entities;
using Automotive.Marketplace.Domain.Enums;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace Automotive.Marketplace.Infrastructure.Data.Configuration;

public class ListingConfiguration : IEntityTypeConfiguration<Listing>
{
    public void Configure(EntityTypeBuilder<Listing> builder)
    {
        builder.HasOne(listing => listing.Variant)
            .WithMany(variant => variant.Listings)
            .HasForeignKey(listing => listing.VariantId);

        builder.HasOne(listing => listing.Drivetrain)
            .WithMany()
            .HasForeignKey(listing => listing.DrivetrainId);

        builder.HasOne(listing => listing.Seller)
            .WithMany(user => user.Listings)
            .HasForeignKey(listing => listing.SellerId);

        builder.Property(listing => listing.Status)
            .HasConversion(
                statusEnum => statusEnum.ToString(),
                statusString => (Status)Enum.Parse(typeof(Status), statusString)
            );
    }
}
```

---

## Task 5: Infrastructure — AutomotiveContext + Builders

**Files:**
- Modify: `Automotive.Marketplace.Infrastructure/Data/DatabaseContext/AutomotiveContext.cs`
- Create: `Automotive.Marketplace.Infrastructure/Data/Builders/VariantBuilder.cs`
- Modify: `Automotive.Marketplace.Infrastructure/Data/Builders/ListingBuilder.cs`

- [ ] **Step 1: Replace AutomotiveContext.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/DatabaseContext/AutomotiveContext.cs
using Automotive.Marketplace.Domain.Entities;
using Automotive.Marketplace.Infrastructure.Data.Configuration;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Infrastructure.Data.DatabaseContext;

public class AutomotiveContext(DbContextOptions options) : DbContext(options)
{
    public DbSet<Variant> Variants { get; set; }

    public DbSet<Drivetrain> Drivetrains { get; set; }

    public DbSet<Fuel> Fuels { get; set; }

    public DbSet<Transmission> Transmissions { get; set; }

    public DbSet<BodyType> BodyTypes { get; set; }

    public DbSet<FuelTranslation> FuelTranslations { get; set; }

    public DbSet<TransmissionTranslation> TransmissionTranslations { get; set; }

    public DbSet<BodyTypeTranslation> BodyTypeTranslations { get; set; }

    public DbSet<DrivetrainTranslation> DrivetrainTranslations { get; set; }

    public DbSet<Image> Images { get; set; }

    public DbSet<Listing> Listings { get; set; }

    public DbSet<User> Users { get; set; }

    public DbSet<UserPermission> UserPermissions { get; set; }

    public DbSet<Make> Makes { get; set; }

    public DbSet<Model> Models { get; set; }

    public DbSet<RefreshToken> RefreshTokens { get; set; }

    public DbSet<Conversation> Conversations { get; set; }

    public DbSet<Message> Messages { get; set; }

    protected override void OnModelCreating(ModelBuilder modelBuilder)
    {
        modelBuilder.ApplyConfigurationsFromAssembly(typeof(VariantConfiguration).Assembly);
    }
}
```

- [ ] **Step 2: Create VariantBuilder.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/Builders/VariantBuilder.cs
using System.Linq.Expressions;
using Automotive.Marketplace.Domain.Entities;
using Bogus;

namespace Automotive.Marketplace.Infrastructure.Data.Builders;

public class VariantBuilder
{
    private readonly Faker<Variant> _faker;

    public VariantBuilder()
    {
        _faker = new Faker<Variant>()
            .RuleFor(v => v.Id, f => f.Random.Guid())
            .RuleFor(v => v.Year, f => f.Random.Int(1980, 2025))
            .RuleFor(v => v.PowerKw, f => f.Random.Int(40, 500))
            .RuleFor(v => v.EngineSizeMl, f => f.Random.Int(800, 6000))
            .RuleFor(v => v.DoorCount, f => f.Random.Int(2, 6))
            .RuleFor(v => v.IsCustom, _ => false);
    }

    public VariantBuilder WithYear(int year)
    {
        _faker.RuleFor(v => v.Year, year);
        return this;
    }

    public VariantBuilder WithModel(Guid modelId)
    {
        _faker.RuleFor(v => v.ModelId, modelId);
        return this;
    }

    public VariantBuilder WithFuel(Guid fuelId)
    {
        _faker.RuleFor(v => v.FuelId, fuelId);
        return this;
    }

    public VariantBuilder WithTransmission(Guid transmissionId)
    {
        _faker.RuleFor(v => v.TransmissionId, transmissionId);
        return this;
    }

    public VariantBuilder WithBodyType(Guid bodyTypeId)
    {
        _faker.RuleFor(v => v.BodyTypeId, bodyTypeId);
        return this;
    }

    public VariantBuilder With<T>(Expression<Func<Variant, T>> property, T value)
    {
        _faker.RuleFor(property, value);
        return this;
    }

    public Variant Build() => _faker.Generate();

    public List<Variant> Build(int count) => _faker.Generate(count);
}
```

- [ ] **Step 3: Replace ListingBuilder.cs**

Remove `Power` and `EngineSize` rules (moved to Variant). Rename `WithCar` to `WithVariant`. Add `WithDrivetrain`.

```csharp
// Automotive.Marketplace.Infrastructure/Data/Builders/ListingBuilder.cs
using System.Linq.Expressions;
using Automotive.Marketplace.Domain.Entities;
using Automotive.Marketplace.Domain.Enums;
using Bogus;

namespace Automotive.Marketplace.Infrastructure.Data.Builders;

public class ListingBuilder
{
    private readonly Faker<Listing> _faker;

    public ListingBuilder()
    {
        _faker = new Faker<Listing>()
            .RuleFor(listing => listing.Id, f => f.Random.Guid())
            .RuleFor(listing => listing.Price, f => f.Random.Decimal(100, 50000))
            .RuleFor(listing => listing.Description, f => f.Lorem.Sentences(2))
            .RuleFor(listing => listing.City, f => f.Address.City())
            .RuleFor(listing => listing.Status, Status.Available)
            .RuleFor(listing => listing.Vin, f => f.Vehicle.Vin())
            .RuleFor(listing => listing.Colour, f => f.Commerce.Color())
            .RuleFor(listing => listing.IsUsed, f => f.Random.Bool())
            .RuleFor(listing => listing.Mileage, f => f.Random.Int(10000, 400000))
            .RuleFor(listing => listing.IsSteeringWheelRight, f => f.Random.Bool());
    }

    public ListingBuilder WithPrice(decimal price)
    {
        _faker.RuleFor(listing => listing.Price, price);
        return this;
    }

    public ListingBuilder WithListing(Guid listingId)
    {
        _faker.RuleFor(listing => listing.Id, listingId);
        return this;
    }

    public ListingBuilder WithSeller(Guid sellerId)
    {
        _faker.RuleFor(listing => listing.SellerId, sellerId);
        return this;
    }

    public ListingBuilder WithUsed(bool isUsed)
    {
        _faker.RuleFor(listing => listing.IsUsed, isUsed);
        return this;
    }

    public ListingBuilder WithVariant(Guid variantId)
    {
        _faker.RuleFor(listing => listing.VariantId, variantId);
        return this;
    }

    public ListingBuilder WithDrivetrain(Guid drivetrainId)
    {
        _faker.RuleFor(listing => listing.DrivetrainId, drivetrainId);
        return this;
    }

    public ListingBuilder With<T>(Expression<Func<Listing, T>> property, T value)
    {
        _faker.RuleFor(property, value);
        return this;
    }

    public Listing Build() => _faker.Generate();

    public List<Listing> Build(int count) => _faker.Generate(count);
}
```

---

## Task 6: Infrastructure — Lookup Seeders + VariantSeeder + ListingSeeder + ServiceExtensions

**Files:**
- Delete: `Automotive.Marketplace.Infrastructure/Data/Seeders/CarSeeder.cs`
- Delete: `Automotive.Marketplace.Infrastructure/Data/Builders/CarBuilder.cs`
- Create: `Automotive.Marketplace.Infrastructure/Data/Seeders/FuelSeeder.cs`
- Create: `Automotive.Marketplace.Infrastructure/Data/Seeders/TransmissionSeeder.cs`
- Create: `Automotive.Marketplace.Infrastructure/Data/Seeders/BodyTypeSeeder.cs`
- Create: `Automotive.Marketplace.Infrastructure/Data/Seeders/DrivetrainSeeder.cs`
- Create: `Automotive.Marketplace.Infrastructure/Data/Seeders/VariantSeeder.cs`
- Modify: `Automotive.Marketplace.Infrastructure/Data/Seeders/ListingSeeder.cs`
- Modify: `Automotive.Marketplace.Infrastructure/ServiceExtensions.cs`

- [ ] **Step 1: Delete old builders/seeders**

```bash
rm Automotive.Marketplace.Infrastructure/Data/Builders/CarBuilder.cs
rm Automotive.Marketplace.Infrastructure/Data/Seeders/CarSeeder.cs
```

- [ ] **Step 2: Create FuelSeeder.cs**

Uses fixed GUIDs so re-seeding is idempotent and other seeders can reference the same IDs.

```csharp
// Automotive.Marketplace.Infrastructure/Data/Seeders/FuelSeeder.cs
using Automotive.Marketplace.Domain.Entities;
using Automotive.Marketplace.Infrastructure.Data.DatabaseContext;
using Automotive.Marketplace.Infrastructure.Interfaces;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Infrastructure.Data.Seeders;

public class FuelSeeder(AutomotiveContext context) : IDevelopmentSeeder
{
    private static readonly List<(Guid Id, string En, string Lt)> Fuels =
    [
        (Guid.Parse("11111111-0000-0000-0000-000000000001"), "Diesel", "Dyzelinas"),
        (Guid.Parse("11111111-0000-0000-0000-000000000002"), "Petrol", "Benzinas"),
        (Guid.Parse("11111111-0000-0000-0000-000000000003"), "Electric", "Elektra"),
        (Guid.Parse("11111111-0000-0000-0000-000000000004"), "Petrol/LPG", "Benzinas/Dujos"),
        (Guid.Parse("11111111-0000-0000-0000-000000000005"), "Petrol/Electric", "Benzinas/Elektra"),
        (Guid.Parse("11111111-0000-0000-0000-000000000006"), "Plug-in Hybrid", "Įkraunamas hibridas"),
    ];

    public async Task SeedAsync(CancellationToken cancellationToken)
    {
        if (await context.Set<Fuel>().AnyAsync(cancellationToken))
            return;

        foreach (var (id, en, lt) in Fuels)
        {
            var fuel = new Fuel
            {
                Id = id,
                Name = en,
                Translations =
                [
                    new FuelTranslation { Id = Guid.NewGuid(), FuelId = id, LanguageCode = "en", Name = en },
                    new FuelTranslation { Id = Guid.NewGuid(), FuelId = id, LanguageCode = "lt", Name = lt },
                ]
            };
            await context.AddAsync(fuel, cancellationToken);
        }

        await context.SaveChangesAsync(cancellationToken);
    }
}
```

- [ ] **Step 3: Create TransmissionSeeder.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/Seeders/TransmissionSeeder.cs
using Automotive.Marketplace.Domain.Entities;
using Automotive.Marketplace.Infrastructure.Data.DatabaseContext;
using Automotive.Marketplace.Infrastructure.Interfaces;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Infrastructure.Data.Seeders;

public class TransmissionSeeder(AutomotiveContext context) : IDevelopmentSeeder
{
    private static readonly List<(Guid Id, string En, string Lt)> Transmissions =
    [
        (Guid.Parse("22222222-0000-0000-0000-000000000001"), "Automatic", "Automatinis"),
        (Guid.Parse("22222222-0000-0000-0000-000000000002"), "Manual", "Mechaninis"),
    ];

    public async Task SeedAsync(CancellationToken cancellationToken)
    {
        if (await context.Set<Transmission>().AnyAsync(cancellationToken))
            return;

        foreach (var (id, en, lt) in Transmissions)
        {
            var transmission = new Transmission
            {
                Id = id,
                Name = en,
                Translations =
                [
                    new TransmissionTranslation { Id = Guid.NewGuid(), TransmissionId = id, LanguageCode = "en", Name = en },
                    new TransmissionTranslation { Id = Guid.NewGuid(), TransmissionId = id, LanguageCode = "lt", Name = lt },
                ]
            };
            await context.AddAsync(transmission, cancellationToken);
        }

        await context.SaveChangesAsync(cancellationToken);
    }
}
```

- [ ] **Step 4: Create BodyTypeSeeder.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/Seeders/BodyTypeSeeder.cs
using Automotive.Marketplace.Domain.Entities;
using Automotive.Marketplace.Infrastructure.Data.DatabaseContext;
using Automotive.Marketplace.Infrastructure.Interfaces;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Infrastructure.Data.Seeders;

public class BodyTypeSeeder(AutomotiveContext context) : IDevelopmentSeeder
{
    private static readonly List<(Guid Id, string En, string Lt)> BodyTypes =
    [
        (Guid.Parse("33333333-0000-0000-0000-000000000001"), "Sedan", "Sedanas"),
        (Guid.Parse("33333333-0000-0000-0000-000000000002"), "Hatchback", "Hečbekas"),
        (Guid.Parse("33333333-0000-0000-0000-000000000003"), "Wagon", "Universalas"),
        (Guid.Parse("33333333-0000-0000-0000-000000000004"), "Coupe", "Kupė"),
        (Guid.Parse("33333333-0000-0000-0000-000000000005"), "SUV", "Visureigis"),
        (Guid.Parse("33333333-0000-0000-0000-000000000006"), "Minivan", "Minivenas"),
        (Guid.Parse("33333333-0000-0000-0000-000000000007"), "Crossover", "Krosoveris"),
    ];

    public async Task SeedAsync(CancellationToken cancellationToken)
    {
        if (await context.Set<BodyType>().AnyAsync(cancellationToken))
            return;

        foreach (var (id, en, lt) in BodyTypes)
        {
            var bodyType = new BodyType
            {
                Id = id,
                Name = en,
                Translations =
                [
                    new BodyTypeTranslation { Id = Guid.NewGuid(), BodyTypeId = id, LanguageCode = "en", Name = en },
                    new BodyTypeTranslation { Id = Guid.NewGuid(), BodyTypeId = id, LanguageCode = "lt", Name = lt },
                ]
            };
            await context.AddAsync(bodyType, cancellationToken);
        }

        await context.SaveChangesAsync(cancellationToken);
    }
}
```

- [ ] **Step 5: Create DrivetrainSeeder.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/Seeders/DrivetrainSeeder.cs
using Automotive.Marketplace.Domain.Entities;
using Automotive.Marketplace.Infrastructure.Data.DatabaseContext;
using Automotive.Marketplace.Infrastructure.Interfaces;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Infrastructure.Data.Seeders;

public class DrivetrainSeeder(AutomotiveContext context) : IDevelopmentSeeder
{
    private static readonly List<(Guid Id, string En, string Lt)> Drivetrains =
    [
        (Guid.Parse("44444444-0000-0000-0000-000000000001"), "FWD", "Priekinis pavara"),
        (Guid.Parse("44444444-0000-0000-0000-000000000002"), "RWD", "Galinė pavara"),
        (Guid.Parse("44444444-0000-0000-0000-000000000003"), "AWD", "Visų ratų pavara"),
    ];

    public async Task SeedAsync(CancellationToken cancellationToken)
    {
        if (await context.Set<Drivetrain>().AnyAsync(cancellationToken))
            return;

        foreach (var (id, en, lt) in Drivetrains)
        {
            var drivetrain = new Drivetrain
            {
                Id = id,
                Name = en,
                Translations =
                [
                    new DrivetrainTranslation { Id = Guid.NewGuid(), DrivetrainId = id, LanguageCode = "en", Name = en },
                    new DrivetrainTranslation { Id = Guid.NewGuid(), DrivetrainId = id, LanguageCode = "lt", Name = lt },
                ]
            };
            await context.AddAsync(drivetrain, cancellationToken);
        }

        await context.SaveChangesAsync(cancellationToken);
    }
}
```

- [ ] **Step 6: Create VariantSeeder.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/Seeders/VariantSeeder.cs
using Automotive.Marketplace.Domain.Entities;
using Automotive.Marketplace.Infrastructure.Data.Builders;
using Automotive.Marketplace.Infrastructure.Data.DatabaseContext;
using Automotive.Marketplace.Infrastructure.Interfaces;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Infrastructure.Data.Seeders;

public class VariantSeeder(AutomotiveContext context) : IDevelopmentSeeder
{
    public async Task SeedAsync(CancellationToken cancellationToken)
    {
        if (await context.Set<Variant>().AnyAsync(cancellationToken))
            return;

        var models = await context.Set<Model>().ToListAsync(cancellationToken);
        var fuels = await context.Set<Fuel>().ToListAsync(cancellationToken);
        var transmissions = await context.Set<Transmission>().ToListAsync(cancellationToken);
        var bodyTypes = await context.Set<BodyType>().ToListAsync(cancellationToken);

        var random = new Random(42);

        foreach (var model in models)
        {
            for (int i = 0; i < 3; i++)
            {
                var variant = new VariantBuilder()
                    .WithModel(model.Id)
                    .WithFuel(fuels[random.Next(fuels.Count)].Id)
                    .WithTransmission(transmissions[random.Next(transmissions.Count)].Id)
                    .WithBodyType(bodyTypes[random.Next(bodyTypes.Count)].Id)
                    .Build();

                await context.AddAsync(variant, cancellationToken);
            }
        }

        await context.SaveChangesAsync(cancellationToken);
    }
}
```

- [ ] **Step 7: Update ListingSeeder.cs**

```csharp
// Automotive.Marketplace.Infrastructure/Data/Seeders/ListingSeeder.cs
using Automotive.Marketplace.Domain.Entities;
using Automotive.Marketplace.Infrastructure.Data.Builders;
using Automotive.Marketplace.Infrastructure.Data.DatabaseContext;
using Automotive.Marketplace.Infrastructure.Interfaces;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Infrastructure.Data.Seeders;

public class ListingSeeder(AutomotiveContext context) : IDevelopmentSeeder
{
    public async Task SeedAsync(CancellationToken cancellationToken)
    {
        if (await context.Set<Listing>().AnyAsync(cancellationToken))
            return;

        var users = await context.Set<User>().ToListAsync(cancellationToken);
        var variants = await context.Set<Variant>().ToListAsync(cancellationToken);
        var drivetrains = await context.Set<Drivetrain>().ToListAsync(cancellationToken);

        for (int i = 0; i < variants.Count; i++)
        {
            var user = users.Skip(i % users.Count).First();
            var drivetrain = drivetrains[i % drivetrains.Count];

            var listing = new ListingBuilder()
                .WithVariant(variants[i].Id)
                .WithSeller(user.Id)
                .WithDrivetrain(drivetrain.Id)
                .Build();

            await context.AddAsync(listing, cancellationToken);
        }

        await context.SaveChangesAsync(cancellationToken);
    }
}
```

- [ ] **Step 8: Update ServiceExtensions.cs**

Registration order matters: lookup seeders before VariantSeeder, VariantSeeder before ListingSeeder.

```csharp
// Automotive.Marketplace.Infrastructure/ServiceExtensions.cs
using Amazon.S3;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Application.Interfaces.Services;
using Automotive.Marketplace.Infrastructure.Data;
using Automotive.Marketplace.Infrastructure.Data.DatabaseContext;
using Automotive.Marketplace.Infrastructure.Data.Seeders;
using Automotive.Marketplace.Infrastructure.Interfaces;
using Automotive.Marketplace.Infrastructure.Services;
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;

namespace Automotive.Marketplace.Infrastructure;

public static class ServiceExtensions
{
    public static void ConfigureInfrastructure(this IServiceCollection services, IConfiguration configuration, string? connectionString)
    {
        services.AddDbContext<AutomotiveContext>(opt => opt
            .UseLazyLoadingProxies()
            .UseNpgsql(connectionString));
        services.AddScoped<ITokenService, TokenService>();
        services.AddScoped<IPasswordHasher, PasswordHasher>();
        services.AddScoped<IRepository, Repository>();
        services.AddScoped<IImageStorageService, S3ImageStorageService>();

        services.AddScoped<IDevelopmentSeeder, UserSeeder>();
        services.AddScoped<IDevelopmentSeeder, MakeSeeder>();
        services.AddScoped<IDevelopmentSeeder, ModelSeeder>();
        services.AddScoped<IDevelopmentSeeder, FuelSeeder>();
        services.AddScoped<IDevelopmentSeeder, TransmissionSeeder>();
        services.AddScoped<IDevelopmentSeeder, BodyTypeSeeder>();
        services.AddScoped<IDevelopmentSeeder, DrivetrainSeeder>();
        services.AddScoped<IDevelopmentSeeder, VariantSeeder>();
        services.AddScoped<IDevelopmentSeeder, ListingSeeder>();

        var minioServerURL = configuration["MinIO:ServerURL"];
        var accessKey = configuration["MinIO:AccessKey"];
        var secretKey = configuration["MinIO:SecretKey"];

        services.AddSingleton<IAmazonS3>(opt =>
        {
            var config = new AmazonS3Config
            {
                ServiceURL = minioServerURL,
                ForcePathStyle = true,
                UseHttp = true,
            };

            return new AmazonS3Client(accessKey, secretKey, config);
        });
    }
}
```

---

## Task 7: Infrastructure — EF Migration

**Files:**
- New migration in `Automotive.Marketplace.Infrastructure/Migrations/`

- [ ] **Step 1: Verify the solution builds**

```bash
dotnet build Automotive.Marketplace.sln
```
Expected: Build succeeded, 0 errors.

- [ ] **Step 2: Add EF migration**

Run from the repo root (requires `dotnet-ef` tool to be installed).

```bash
dotnet ef migrations add VariantI18nRefactor   --project Automotive.Marketplace.Infrastructure   --startup-project Automotive.Marketplace.Server
```
Expected: A new migration file appears in `Automotive.Marketplace.Infrastructure/Migrations/`.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "refactor: domain + infra — Variant entity, Drivetrain entity, i18n translation tables

- Replace Car entity with Variant (Year:int, IsCustom, PowerKw, EngineSizeMl)
- Add Drivetrain entity (replaces Drivetrain enum); FK on Listing
- Add FuelTranslation, TransmissionTranslation, BodyTypeTranslation, DrivetrainTranslation
- Delete Fuel/BodyType/Transmission/Drivetrain enums
- Add VariantBuilder; update ListingBuilder (WithVariant, WithDrivetrain)
- Add FuelSeeder, TransmissionSeeder, BodyTypeSeeder, DrivetrainSeeder, VariantSeeder
- Update ListingSeeder to use VariantId + DrivetrainId
- Add EF migration VariantI18nRefactor

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
```

---

## Task 8: Application — Enum Features (DB Query + Id in Response)

**Files:**
- Modify: `Automotive.Marketplace.Application/Features/EnumFeatures/GetFuelTypes/GetFuelTypesResponse.cs`
- Modify: `Automotive.Marketplace.Application/Features/EnumFeatures/GetFuelTypes/GetFuelTypesQueryHandler.cs`
- Modify: `Automotive.Marketplace.Application/Features/EnumFeatures/GetBodyTypes/GetBodyTypesResponse.cs`
- Modify: `Automotive.Marketplace.Application/Features/EnumFeatures/GetBodyTypes/GetBodyTypesQueryHandler.cs`
- Modify: `Automotive.Marketplace.Application/Features/EnumFeatures/GetTransmissionTypes/GetTransmissionTypesResponse.cs`
- Modify: `Automotive.Marketplace.Application/Features/EnumFeatures/GetTransmissionTypes/GetTransmissionTypesQueryHandler.cs`
- Modify: `Automotive.Marketplace.Application/Features/EnumFeatures/GetDrivetrainTypes/GetDrivetrainTypesResponse.cs`
- Modify: `Automotive.Marketplace.Application/Features/EnumFeatures/GetDrivetrainTypes/GetDrivetrainTypesQueryHandler.cs`
- Modify: `Automotive.Marketplace.Application/Mappings/EnumMappings.cs`

- [ ] **Step 1: Update all four response records to add Id**

```csharp
// Automotive.Marketplace.Application/Features/EnumFeatures/GetFuelTypes/GetFuelTypesResponse.cs
namespace Automotive.Marketplace.Application.Features.EnumFeatures.GetFuelTypes;

public sealed record GetFuelTypesResponse
{
    public Guid Id { get; set; }

    public string FuelType { get; set; } = string.Empty;
}
```

```csharp
// Automotive.Marketplace.Application/Features/EnumFeatures/GetBodyTypes/GetBodyTypesResponse.cs
namespace Automotive.Marketplace.Application.Features.EnumFeatures.GetBodyTypes;

public sealed record GetBodyTypesResponse
{
    public Guid Id { get; set; }

    public string BodyType { get; set; } = string.Empty;
}
```

```csharp
// Automotive.Marketplace.Application/Features/EnumFeatures/GetTransmissionTypes/GetTransmissionTypesResponse.cs
namespace Automotive.Marketplace.Application.Features.EnumFeatures.GetTransmissionTypes;

public sealed record GetTransmissionTypesResponse
{
    public Guid Id { get; set; }

    public string TransmissionType { get; set; } = string.Empty;
}
```

```csharp
// Automotive.Marketplace.Application/Features/EnumFeatures/GetDrivetrainTypes/GetDrivetrainTypesResponse.cs
namespace Automotive.Marketplace.Application.Features.EnumFeatures.GetDrivetrainTypes;

public sealed record GetDrivetrainTypesResponse
{
    public Guid Id { get; set; }

    public string DrivetrainType { get; set; } = string.Empty;
}
```

- [ ] **Step 2: Update all four handlers to query from DB**

```csharp
// Automotive.Marketplace.Application/Features/EnumFeatures/GetFuelTypes/GetFuelTypesQueryHandler.cs
using AutoMapper;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Domain.Entities;
using MediatR;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Application.Features.EnumFeatures.GetFuelTypes;

public class GetFuelTypesQueryHandler(
    IMapper mapper,
    IRepository repository) : IRequestHandler<GetFuelTypesQuery, IEnumerable<GetFuelTypesResponse>>
{
    public async Task<IEnumerable<GetFuelTypesResponse>> Handle(
        GetFuelTypesQuery request,
        CancellationToken cancellationToken)
    {
        var fuels = await repository.AsQueryable<Fuel>().ToListAsync(cancellationToken);
        return mapper.Map<IEnumerable<GetFuelTypesResponse>>(fuels);
    }
}
```

```csharp
// Automotive.Marketplace.Application/Features/EnumFeatures/GetBodyTypes/GetBodyTypesQueryHandler.cs
using AutoMapper;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Domain.Entities;
using MediatR;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Application.Features.EnumFeatures.GetBodyTypes;

public class GetBodyTypesQueryHandler(
    IMapper mapper,
    IRepository repository) : IRequestHandler<GetBodyTypesQuery, IEnumerable<GetBodyTypesResponse>>
{
    public async Task<IEnumerable<GetBodyTypesResponse>> Handle(
        GetBodyTypesQuery request,
        CancellationToken cancellationToken)
    {
        var bodyTypes = await repository.AsQueryable<BodyType>().ToListAsync(cancellationToken);
        return mapper.Map<IEnumerable<GetBodyTypesResponse>>(bodyTypes);
    }
}
```

```csharp
// Automotive.Marketplace.Application/Features/EnumFeatures/GetTransmissionTypes/GetTransmissionTypesQueryHandler.cs
using AutoMapper;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Domain.Entities;
using MediatR;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Application.Features.EnumFeatures.GetTransmissionTypes;

public class GetTransmissionTypesQueryHandler(
    IMapper mapper,
    IRepository repository) : IRequestHandler<GetTransmissionTypesQuery, IEnumerable<GetTransmissionTypesResponse>>
{
    public async Task<IEnumerable<GetTransmissionTypesResponse>> Handle(
        GetTransmissionTypesQuery request,
        CancellationToken cancellationToken)
    {
        var transmissions = await repository.AsQueryable<Transmission>().ToListAsync(cancellationToken);
        return mapper.Map<IEnumerable<GetTransmissionTypesResponse>>(transmissions);
    }
}
```

```csharp
// Automotive.Marketplace.Application/Features/EnumFeatures/GetDrivetrainTypes/GetDrivetrainTypesQueryHandler.cs
using AutoMapper;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Domain.Entities;
using MediatR;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Application.Features.EnumFeatures.GetDrivetrainTypes;

public class GetDrivetrainTypesQueryHandler(
    IMapper mapper,
    IRepository repository) : IRequestHandler<GetDrivetrainTypesQuery, IEnumerable<GetDrivetrainTypesResponse>>
{
    public async Task<IEnumerable<GetDrivetrainTypesResponse>> Handle(
        GetDrivetrainTypesQuery request,
        CancellationToken cancellationToken)
    {
        var drivetrains = await repository.AsQueryable<Drivetrain>().ToListAsync(cancellationToken);
        return mapper.Map<IEnumerable<GetDrivetrainTypesResponse>>(drivetrains);
    }
}
```

- [ ] **Step 3: Update EnumMappings.cs**

Map from entities (not C# enums). The `Id` field maps automatically by convention (`src.Id → dest.Id`).

```csharp
// Automotive.Marketplace.Application/Mappings/EnumMappings.cs
using AutoMapper;
using Automotive.Marketplace.Application.Features.EnumFeatures.GetBodyTypes;
using Automotive.Marketplace.Application.Features.EnumFeatures.GetDrivetrainTypes;
using Automotive.Marketplace.Application.Features.EnumFeatures.GetFuelTypes;
using Automotive.Marketplace.Application.Features.EnumFeatures.GetTransmissionTypes;
using Automotive.Marketplace.Domain.Entities;

namespace Automotive.Marketplace.Application.Mappings;

public class EnumMappings : Profile
{
    public EnumMappings()
    {
        CreateMap<Transmission, GetTransmissionTypesResponse>()
            .ForMember(dest => dest.TransmissionType, opt => opt.MapFrom(src => src.Name));

        CreateMap<Fuel, GetFuelTypesResponse>()
            .ForMember(dest => dest.FuelType, opt => opt.MapFrom(src => src.Name));

        CreateMap<BodyType, GetBodyTypesResponse>()
            .ForMember(dest => dest.BodyType, opt => opt.MapFrom(src => src.Name));

        CreateMap<Drivetrain, GetDrivetrainTypesResponse>()
            .ForMember(dest => dest.DrivetrainType, opt => opt.MapFrom(src => src.Name));
    }
}
```

---

## Task 9: Application — Variant Features (CRUD + GetByModel)

**Files:**
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/CreateVariant/CreateVariantCommand.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/CreateVariant/CreateVariantCommandHandler.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/DeleteVariant/DeleteVariantCommand.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/DeleteVariant/DeleteVariantCommandHandler.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/GetAllVariants/GetAllVariantsQuery.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/GetAllVariants/GetAllVariantsQueryHandler.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/GetAllVariants/GetAllVariantsResponse.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantById/GetVariantByIdQuery.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantById/GetVariantByIdQueryHandler.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantById/GetVariantByIdResponse.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/UpdateVariant/UpdateVariantCommand.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/UpdateVariant/UpdateVariantCommandHandler.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantsByModel/GetVariantsByModelQuery.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantsByModel/GetVariantsByModelQueryHandler.cs`
- Create: `Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantsByModel/GetVariantsByModelResponse.cs`
- Create: `Automotive.Marketplace.Application/Mappings/VariantMappings.cs`
- Delete: `Automotive.Marketplace.Application/Features/CarFeatures/` (entire folder)
- Delete: `Automotive.Marketplace.Application/Mappings/CarMappings.cs`

- [ ] **Step 1: Create CreateVariantCommand.cs**

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/CreateVariant/CreateVariantCommand.cs
using MediatR;

namespace Automotive.Marketplace.Application.Features.VariantFeatures.CreateVariant;

public sealed record CreateVariantCommand : IRequest
{
    public int Year { get; set; }

    public Guid FuelId { get; set; }

    public Guid TransmissionId { get; set; }

    public Guid BodyTypeId { get; set; }

    public int DoorCount { get; set; }

    public int PowerKw { get; set; }

    public int EngineSizeMl { get; set; }

    public bool IsCustom { get; set; }

    public Guid ModelId { get; set; }
}
```

- [ ] **Step 2: Create CreateVariantCommandHandler.cs**

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/CreateVariant/CreateVariantCommandHandler.cs
using AutoMapper;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Domain.Entities;
using MediatR;

namespace Automotive.Marketplace.Application.Features.VariantFeatures.CreateVariant;

public class CreateVariantCommandHandler(
    IMapper mapper,
    IRepository repository) : IRequestHandler<CreateVariantCommand>
{
    public async Task Handle(CreateVariantCommand request, CancellationToken cancellationToken)
    {
        var variant = mapper.Map<Variant>(request);
        await repository.CreateAsync(variant, cancellationToken);
    }
}
```

- [ ] **Step 3: Create DeleteVariantCommand.cs + Handler**

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/DeleteVariant/DeleteVariantCommand.cs
using MediatR;

namespace Automotive.Marketplace.Application.Features.VariantFeatures.DeleteVariant;

public sealed record DeleteVariantCommand : IRequest
{
    public Guid Id { get; set; }
}
```

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/DeleteVariant/DeleteVariantCommandHandler.cs
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Domain.Entities;
using MediatR;

namespace Automotive.Marketplace.Application.Features.VariantFeatures.DeleteVariant;

public class DeleteVariantCommandHandler(IRepository repository) : IRequestHandler<DeleteVariantCommand>
{
    public async Task Handle(DeleteVariantCommand request, CancellationToken cancellationToken)
    {
        var variant = await repository.GetByIdAsync<Variant>(request.Id, cancellationToken);
        await repository.DeleteAsync(variant, cancellationToken);
    }
}
```

- [ ] **Step 4: Create GetAllVariants files**

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/GetAllVariants/GetAllVariantsQuery.cs
using MediatR;

namespace Automotive.Marketplace.Application.Features.VariantFeatures.GetAllVariants;

public sealed record GetAllVariantsQuery : IRequest<IEnumerable<GetAllVariantsResponse>>;
```

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/GetAllVariants/GetAllVariantsResponse.cs
namespace Automotive.Marketplace.Application.Features.VariantFeatures.GetAllVariants;

public sealed record GetAllVariantsResponse
{
    public Guid Id { get; set; }

    public int Year { get; set; }

    public string Make { get; set; } = string.Empty;

    public string Model { get; set; } = string.Empty;

    public string FuelType { get; set; } = string.Empty;

    public string Transmission { get; set; } = string.Empty;
}
```

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/GetAllVariants/GetAllVariantsQueryHandler.cs
using AutoMapper;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Domain.Entities;
using MediatR;

namespace Automotive.Marketplace.Application.Features.VariantFeatures.GetAllVariants;

public class GetAllVariantsQueryHandler(
    IMapper mapper,
    IRepository repository) : IRequestHandler<GetAllVariantsQuery, IEnumerable<GetAllVariantsResponse>>
{
    public async Task<IEnumerable<GetAllVariantsResponse>> Handle(
        GetAllVariantsQuery request,
        CancellationToken cancellationToken)
    {
        var variants = await repository.GetAllAsync<Variant>(cancellationToken);
        variants = [.. variants.OrderByDescending(v => v.Year)];
        return mapper.Map<IEnumerable<GetAllVariantsResponse>>(variants);
    }
}
```

- [ ] **Step 5: Create GetVariantById files**

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantById/GetVariantByIdQuery.cs
using MediatR;

namespace Automotive.Marketplace.Application.Features.VariantFeatures.GetVariantById;

public sealed record GetVariantByIdQuery : IRequest<GetVariantByIdResponse>
{
    public Guid Id { get; set; }
}
```

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantById/GetVariantByIdResponse.cs
namespace Automotive.Marketplace.Application.Features.VariantFeatures.GetVariantById;

public sealed record GetVariantByIdResponse
{
    public Guid Id { get; set; }

    public int Year { get; set; }

    public Guid FuelId { get; set; }

    public string Fuel { get; set; } = string.Empty;

    public Guid TransmissionId { get; set; }

    public string Transmission { get; set; } = string.Empty;

    public Guid BodyTypeId { get; set; }

    public string BodyType { get; set; } = string.Empty;

    public int DoorCount { get; set; }

    public int PowerKw { get; set; }

    public int EngineSizeMl { get; set; }

    public bool IsCustom { get; set; }

    public Guid MakeId { get; set; }

    public Guid ModelId { get; set; }
}
```

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantById/GetVariantByIdQueryHandler.cs
using AutoMapper;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Domain.Entities;
using MediatR;

namespace Automotive.Marketplace.Application.Features.VariantFeatures.GetVariantById;

public class GetVariantByIdQueryHandler(
    IMapper mapper,
    IRepository repository) : IRequestHandler<GetVariantByIdQuery, GetVariantByIdResponse>
{
    public async Task<GetVariantByIdResponse> Handle(GetVariantByIdQuery request, CancellationToken cancellationToken)
    {
        var variant = await repository.GetByIdAsync<Variant>(request.Id, cancellationToken);
        return mapper.Map<GetVariantByIdResponse>(variant);
    }
}
```

- [ ] **Step 6: Create UpdateVariant files**

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/UpdateVariant/UpdateVariantCommand.cs
using MediatR;

namespace Automotive.Marketplace.Application.Features.VariantFeatures.UpdateVariant;

public sealed record UpdateVariantCommand : IRequest
{
    public Guid Id { get; set; }

    public int Year { get; set; }

    public Guid FuelId { get; set; }

    public Guid TransmissionId { get; set; }

    public Guid BodyTypeId { get; set; }

    public int DoorCount { get; set; }

    public int PowerKw { get; set; }

    public int EngineSizeMl { get; set; }

    public bool IsCustom { get; set; }

    public Guid ModelId { get; set; }
}
```

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/UpdateVariant/UpdateVariantCommandHandler.cs
using AutoMapper;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Domain.Entities;
using MediatR;

namespace Automotive.Marketplace.Application.Features.VariantFeatures.UpdateVariant;

public class UpdateVariantCommandHandler(
    IRepository repository,
    IMapper mapper) : IRequestHandler<UpdateVariantCommand>
{
    public async Task Handle(UpdateVariantCommand request, CancellationToken cancellationToken)
    {
        var variant = mapper.Map<Variant>(request);
        await repository.UpdateAsync(variant, cancellationToken);
    }
}
```

- [ ] **Step 7: Create GetVariantsByModel files**

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantsByModel/GetVariantsByModelQuery.cs
using MediatR;

namespace Automotive.Marketplace.Application.Features.VariantFeatures.GetVariantsByModel;

public sealed record GetVariantsByModelQuery : IRequest<IEnumerable<GetVariantsByModelResponse>>
{
    public Guid ModelId { get; set; }

    public int? Year { get; set; }
}
```

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantsByModel/GetVariantsByModelResponse.cs
namespace Automotive.Marketplace.Application.Features.VariantFeatures.GetVariantsByModel;

public sealed record GetVariantsByModelResponse
{
    public Guid Id { get; set; }

    public int Year { get; set; }

    public string FuelType { get; set; } = string.Empty;

    public string Transmission { get; set; } = string.Empty;

    public int DoorCount { get; set; }

    public int PowerKw { get; set; }

    public int EngineSizeMl { get; set; }
}
```

```csharp
// Automotive.Marketplace.Application/Features/VariantFeatures/GetVariantsByModel/GetVariantsByModelQueryHandler.cs
using AutoMapper;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Domain.Entities;
using MediatR;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Application.Features.VariantFeatures.GetVariantsByModel;

public class GetVariantsByModelQueryHandler(
    IMapper mapper,
    IRepository repository) : IRequestHandler<GetVariantsByModelQuery, IEnumerable<GetVariantsByModelResponse>>
{
    public async Task<IEnumerable<GetVariantsByModelResponse>> Handle(
        GetVariantsByModelQuery request,
        CancellationToken cancellationToken)
    {
        var variants = await repository
            .AsQueryable<Variant>()
            .Where(v => v.ModelId == request.ModelId)
            .Where(v => !v.IsCustom)
            .Where(v => request.Year == null || v.Year == request.Year)
            .OrderBy(v => v.Year)
            .ToListAsync(cancellationToken);

        return mapper.Map<IEnumerable<GetVariantsByModelResponse>>(variants);
    }
}
```

- [ ] **Step 8: Create VariantMappings.cs**

```csharp
// Automotive.Marketplace.Application/Mappings/VariantMappings.cs
using AutoMapper;
using Automotive.Marketplace.Application.Features.VariantFeatures.CreateVariant;
using Automotive.Marketplace.Application.Features.VariantFeatures.GetAllVariants;
using Automotive.Marketplace.Application.Features.VariantFeatures.GetVariantById;
using Automotive.Marketplace.Application.Features.VariantFeatures.GetVariantsByModel;
using Automotive.Marketplace.Application.Features.VariantFeatures.UpdateVariant;
using Automotive.Marketplace.Domain.Entities;

namespace Automotive.Marketplace.Application.Mappings;

public class VariantMappings : Profile
{
    public VariantMappings()
    {
        CreateMap<CreateVariantCommand, Variant>();

        CreateMap<UpdateVariantCommand, Variant>();

        CreateMap<Variant, GetVariantByIdResponse>()
            .ForMember(dest => dest.Fuel, opt => opt.MapFrom(src => src.Fuel.Name))
            .ForMember(dest => dest.Transmission, opt => opt.MapFrom(src => src.Transmission.Name))
            .ForMember(dest => dest.BodyType, opt => opt.MapFrom(src => src.BodyType.Name))
            .ForMember(dest => dest.MakeId, opt => opt.MapFrom(src => src.Model.MakeId));

        CreateMap<Variant, GetAllVariantsResponse>()
            .ForMember(dest => dest.Make, opt => opt.MapFrom(src => src.Model.Make.Name))
            .ForMember(dest => dest.Model, opt => opt.MapFrom(src => src.Model.Name))
            .ForMember(dest => dest.FuelType, opt => opt.MapFrom(src => src.Fuel.Name))
            .ForMember(dest => dest.Transmission, opt => opt.MapFrom(src => src.Transmission.Name));

        CreateMap<Variant, GetVariantsByModelResponse>()
            .ForMember(dest => dest.FuelType, opt => opt.MapFrom(src => src.Fuel.Name))
            .ForMember(dest => dest.Transmission, opt => opt.MapFrom(src => src.Transmission.Name));
    }
}
```

- [ ] **Step 9: Delete CarFeatures folder and CarMappings.cs**

```bash
rm -rf Automotive.Marketplace.Application/Features/CarFeatures
rm Automotive.Marketplace.Application/Mappings/CarMappings.cs
```

---

## Task 10: Application — VariantController + Delete CarController

**Files:**
- Create: `Automotive.Marketplace.Server/Controllers/VariantController.cs`
- Delete: `Automotive.Marketplace.Server/Controllers/CarController.cs`

- [ ] **Step 1: Create VariantController.cs**

```csharp
// Automotive.Marketplace.Server/Controllers/VariantController.cs
using Automotive.Marketplace.Application.Features.VariantFeatures.CreateVariant;
using Automotive.Marketplace.Application.Features.VariantFeatures.DeleteVariant;
using Automotive.Marketplace.Application.Features.VariantFeatures.GetAllVariants;
using Automotive.Marketplace.Application.Features.VariantFeatures.GetVariantById;
using Automotive.Marketplace.Application.Features.VariantFeatures.GetVariantsByModel;
using Automotive.Marketplace.Application.Features.VariantFeatures.UpdateVariant;
using Automotive.Marketplace.Domain.Enums;
using Automotive.Marketplace.Server.Attributes;
using MediatR;
using Microsoft.AspNetCore.Mvc;

namespace Automotive.Marketplace.Server.Controllers;

public class VariantController(IMediator mediator) : BaseController
{
    [HttpGet]
    [Protect(Permission.ViewVariants)]
    public async Task<ActionResult<GetVariantByIdResponse>> GetById([FromQuery] GetVariantByIdQuery query, CancellationToken cancellationToken)
    {
        var result = await mediator.Send(query, cancellationToken);
        return Ok(result);
    }

    [HttpGet]
    [Protect(Permission.ViewVariants)]
    public async Task<ActionResult<IEnumerable<GetAllVariantsResponse>>> GetAll(CancellationToken cancellationToken)
    {
        var result = await mediator.Send(new GetAllVariantsQuery(), cancellationToken);
        return Ok(result);
    }

    [HttpGet]
    public async Task<ActionResult<IEnumerable<GetVariantsByModelResponse>>> GetByModel([FromQuery] GetVariantsByModelQuery query, CancellationToken cancellationToken)
    {
        var result = await mediator.Send(query, cancellationToken);
        return Ok(result);
    }

    [HttpPost]
    [Protect(Permission.ManageVariants, Permission.CreateVariants)]
    public async Task<ActionResult> Create([FromBody] CreateVariantCommand command, CancellationToken cancellationToken)
    {
        await mediator.Send(command, cancellationToken);
        return Created();
    }

    [HttpDelete]
    [Protect(Permission.ManageVariants)]
    public async Task<ActionResult> Delete([FromQuery] DeleteVariantCommand command, CancellationToken cancellationToken)
    {
        await mediator.Send(command, cancellationToken);
        return NoContent();
    }

    [HttpPut]
    [Protect(Permission.ManageVariants)]
    public async Task<ActionResult> Update([FromBody] UpdateVariantCommand command, CancellationToken cancellationToken)
    {
        await mediator.Send(command, cancellationToken);
        return NoContent();
    }
}
```

- [ ] **Step 2: Delete CarController.cs**

```bash
rm Automotive.Marketplace.Server/Controllers/CarController.cs
```

---

## Task 11: Application — Update CreateListing (New Shape + Find-or-Create)

**Files:**
- Modify: `Automotive.Marketplace.Application/Features/ListingFeatures/CreateListing/CreateListingCommand.cs`
- Modify: `Automotive.Marketplace.Application/Features/ListingFeatures/CreateListing/CreateListingCommandHandler.cs`

- [ ] **Step 1: Write the failing test first**

Before updating the handler, write the new test cases. Open `CreateListingCommandHandlerTests.cs` and replace the entire file (Task 12 below does this — do Task 12 first, then come back and run these tests to confirm they fail, then implement the handler).

Actually: follow TDD order — go to Task 12 first, write tests, verify they fail (compile errors expected), then complete this task.

- [ ] **Step 2: Replace CreateListingCommand.cs**

```csharp
// Automotive.Marketplace.Application/Features/ListingFeatures/CreateListing/CreateListingCommand.cs
using MediatR;
using Microsoft.AspNetCore.Http;

namespace Automotive.Marketplace.Application.Features.ListingFeatures.CreateListing;

public sealed record CreateListingCommand : IRequest
{
    // Provide VariantId to use an existing variant directly.
    // If null, the fields below are used to find or create a variant.
    public Guid? VariantId { get; set; }

    // Required when VariantId is null:
    public Guid? ModelId { get; set; }

    public Guid? FuelId { get; set; }

    public Guid? TransmissionId { get; set; }

    public Guid? BodyTypeId { get; set; }

    public int? DoorCount { get; set; }

    public int? PowerKw { get; set; }

    public int? EngineSizeMl { get; set; }

    public int? Year { get; set; }

    public bool IsCustom { get; set; }

    // Always required:
    public Guid DrivetrainId { get; set; }

    public decimal Price { get; set; }

    public string? Description { get; set; }

    public string? Colour { get; set; }

    public string? Vin { get; set; }

    public int Mileage { get; set; }

    public bool IsSteeringWheelRight { get; set; }

    public string City { get; set; } = string.Empty;

    public bool IsUsed { get; set; }

    public Guid UserId { get; set; }

    public List<IFormFile> Images { get; set; } = [];
}
```

- [ ] **Step 3: Replace CreateListingCommandHandler.cs**

Find-or-create logic:
1. If `VariantId` provided → use it directly.
2. If `VariantId` null and `IsCustom == true` → always create new Variant.
3. If `VariantId` null and `IsCustom == false` → search for exact match on all 8 fields; use it if found, create if not.

```csharp
// Automotive.Marketplace.Application/Features/ListingFeatures/CreateListing/CreateListingCommandHandler.cs
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Application.Interfaces.Services;
using Automotive.Marketplace.Domain.Entities;
using MediatR;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Application.Features.ListingFeatures.CreateListing;

public class CreateListingCommandHandler(
    IRepository repository,
    IImageStorageService imageStorageService) : IRequestHandler<CreateListingCommand>
{
    public async Task Handle(CreateListingCommand request, CancellationToken cancellationToken)
    {
        var variantId = await ResolveVariantIdAsync(request, cancellationToken);

        var listing = new Listing
        {
            Price = request.Price,
            Description = request.Description,
            Colour = request.Colour,
            Vin = request.Vin,
            Mileage = request.Mileage,
            IsSteeringWheelRight = request.IsSteeringWheelRight,
            City = request.City,
            IsUsed = request.IsUsed,
            SellerId = request.UserId,
            VariantId = variantId,
            DrivetrainId = request.DrivetrainId,
        };

        await repository.CreateAsync(listing, cancellationToken);

        var variant = await repository.GetByIdAsync<Variant>(variantId, cancellationToken);
        var model = await repository.GetByIdAsync<Model>(variant.ModelId, cancellationToken);

        foreach (var imageFile in request.Images)
        {
            var uniqueFileName = $"{Guid.NewGuid()}_{imageFile.FileName}";
            var imageUploadResult = await imageStorageService.UploadImageAsync(imageFile, uniqueFileName);

            var image = new Image
            {
                ListingId = listing.Id,
                BucketName = imageUploadResult.BucketName,
                ObjectKey = imageUploadResult.ObjectKey,
                OriginalFileName = imageFile.FileName,
                ContentType = imageFile.ContentType,
                FileSize = imageUploadResult.FileSize,
                AltText = $"{variant.Year} {model.Make.Name} {model.Name}",
            };

            await repository.CreateAsync(image, cancellationToken);
        }
    }

    private async Task<Guid> ResolveVariantIdAsync(CreateListingCommand request, CancellationToken cancellationToken)
    {
        if (request.VariantId.HasValue)
            return request.VariantId.Value;

        if (request.IsCustom)
            return await CreateVariantAsync(request, cancellationToken);

        var existing = await repository
            .AsQueryable<Variant>()
            .FirstOrDefaultAsync(v =>
                v.ModelId == request.ModelId!.Value &&
                v.FuelId == request.FuelId!.Value &&
                v.TransmissionId == request.TransmissionId!.Value &&
                v.BodyTypeId == request.BodyTypeId!.Value &&
                v.DoorCount == request.DoorCount!.Value &&
                v.PowerKw == request.PowerKw!.Value &&
                v.EngineSizeMl == request.EngineSizeMl!.Value &&
                v.Year == request.Year!.Value &&
                !v.IsCustom,
            cancellationToken);

        return existing?.Id ?? await CreateVariantAsync(request, cancellationToken);
    }

    private async Task<Guid> CreateVariantAsync(CreateListingCommand request, CancellationToken cancellationToken)
    {
        var variant = new Variant
        {
            ModelId = request.ModelId!.Value,
            FuelId = request.FuelId!.Value,
            TransmissionId = request.TransmissionId!.Value,
            BodyTypeId = request.BodyTypeId!.Value,
            DoorCount = request.DoorCount!.Value,
            PowerKw = request.PowerKw!.Value,
            EngineSizeMl = request.EngineSizeMl!.Value,
            Year = request.Year!.Value,
            IsCustom = request.IsCustom,
        };

        await repository.CreateAsync(variant, cancellationToken);
        return variant.Id;
    }
}
```

---

## Task 12: Application — Update GetAllListings + GetListingById + Mappings + UpdateListing

**Files:**
- Modify: `Automotive.Marketplace.Application/Features/ListingFeatures/GetAllListings/GetAllListingsResponse.cs`
- Modify: `Automotive.Marketplace.Application/Features/ListingFeatures/GetAllListings/GetAllListingsQueryHandler.cs`
- Modify: `Automotive.Marketplace.Application/Features/ListingFeatures/GetListingById/GetListingByIdResponse.cs`
- Modify: `Automotive.Marketplace.Application/Features/ListingFeatures/GetListingById/GetListingByIdQueryHandler.cs`
- Modify: `Automotive.Marketplace.Application/Features/ListingFeatures/UpdateListing/UpdateListingCommand.cs`
- Modify: `Automotive.Marketplace.Application/Features/ListingFeatures/UpdateListing/UpdateListingCommandHandler.cs`
- Modify: `Automotive.Marketplace.Application/Mappings/ListingMappings.cs`

- [ ] **Step 1: Update GetAllListingsResponse.cs**

Rename `Power` → `PowerKw`, `EngineSize` → `EngineSizeMl`.

```csharp
// Automotive.Marketplace.Application/Features/ListingFeatures/GetAllListings/GetAllListingsResponse.cs
namespace Automotive.Marketplace.Application.Features.ListingFeatures.GetAllListings;

public sealed record GetAllListingsResponse
{
    public Guid Id { get; set; }

    public bool IsUsed { get; set; }

    public int Year { get; set; }

    public string Make { get; set; } = string.Empty;

    public string Model { get; set; } = string.Empty;

    public int Mileage { get; set; }

    public decimal Price { get; set; }

    public int EngineSizeMl { get; set; }

    public int PowerKw { get; set; }

    public string FuelType { get; set; } = string.Empty;

    public string Transmission { get; set; } = string.Empty;

    public string City { get; set; } = string.Empty;

    public string Description { get; set; } = string.Empty;

    public List<Image> Images { get; set; } = [];

    public sealed class Image
    {
        public string Url { get; set; } = string.Empty;

        public string AltText { get; set; } = string.Empty;
    }
}
```

- [ ] **Step 2: Update GetAllListingsQueryHandler.cs**

All filters that referenced `listing.Car.*` now reference `listing.Variant.*`. `Power`/`EngineSize` filters now use `listing.Variant.PowerKw`.

```csharp
// Automotive.Marketplace.Application/Features/ListingFeatures/GetAllListings/GetAllListingsQueryHandler.cs
using AutoMapper;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Application.Interfaces.Services;
using Automotive.Marketplace.Domain.Entities;
using MediatR;
using Microsoft.EntityFrameworkCore;

namespace Automotive.Marketplace.Application.Features.ListingFeatures.GetAllListings;

public class GetAllListingsQueryHandler(
    IMapper mapper,
    IRepository repository,
    IImageStorageService imageStorageService) : IRequestHandler<GetAllListingsQuery, IEnumerable<GetAllListingsResponse>>
{
    public async Task<IEnumerable<GetAllListingsResponse>> Handle(
        GetAllListingsQuery request,
        CancellationToken cancellationToken)
    {
        var listings = await repository
            .AsQueryable<Listing>()
            .Where(listing => request.MakeId == null || listing.Variant.Model.MakeId == request.MakeId)
            .Where(listing => !request.Models.Any() || request.Models.Contains(listing.Variant.ModelId))
            .Where(listing => request.City == null || request.City.ToLower() == listing.City.ToLower())
            .Where(listing => request.IsUsed == null || request.IsUsed == listing.IsUsed)
            .Where(listing => request.MinYear == null || listing.Variant.Year >= request.MinYear)
            .Where(listing => request.MaxYear == null || listing.Variant.Year <= request.MaxYear)
            .Where(listing => request.MinPrice == null || listing.Price >= request.MinPrice)
            .Where(listing => request.MaxPrice == null || listing.Price <= request.MaxPrice)
            .Where(listing => request.MinMileage == null || listing.Mileage >= request.MinMileage)
            .Where(listing => request.MaxMileage == null || listing.Mileage <= request.MaxMileage)
            .Where(listing => request.MinPower == null || listing.Variant.PowerKw >= request.MinPower)
            .Where(listing => request.MaxPower == null || listing.Variant.PowerKw <= request.MaxPower)
            .ToListAsync(cancellationToken);

        List<GetAllListingsResponse> response = [];
        foreach (var listing in listings)
        {
            var mappedListing = mapper.Map<GetAllListingsResponse>(listing);
            foreach (var image in listing.Images)
            {
                var imageUrl = await imageStorageService.GetPresignedUrlAsync(image.ObjectKey);
                mappedListing.Images.Add(new GetAllListingsResponse.Image
                {
                    Url = imageUrl,
                    AltText = image.AltText
                });
            }
            response.Add(mappedListing);
        }

        return response;
    }
}
```

- [ ] **Step 3: Update GetListingByIdResponse.cs**

```csharp
// Automotive.Marketplace.Application/Features/ListingFeatures/GetListingById/GetListingByIdResponse.cs
namespace Automotive.Marketplace.Application.Features.ListingFeatures.GetListingById;

public sealed record GetListingByIdResponse
{
    public string Make { get; set; } = string.Empty;

    public string Model { get; set; } = string.Empty;

    public decimal Price { get; set; }

    public string? Description { get; set; }

    public string? Colour { get; set; }

    public string? Vin { get; set; }

    public int PowerKw { get; set; }

    public int EngineSizeMl { get; set; }

    public int Mileage { get; set; }

    public bool IsSteeringWheelRight { get; set; }

    public string City { get; set; } = string.Empty;

    public bool IsUsed { get; set; }

    public int Year { get; set; }

    public string Transmission { get; set; } = string.Empty;

    public string Fuel { get; set; } = string.Empty;

    public int DoorCount { get; set; }

    public string BodyType { get; set; } = string.Empty;

    public string Drivetrain { get; set; } = string.Empty;

    public string Seller { get; set; } = string.Empty;

    public Guid SellerId { get; set; }

    public List<Image> Images { get; set; } = [];

    public sealed class Image
    {
        public string Url { get; set; } = string.Empty;

        public string AltText { get; set; } = string.Empty;
    }
}
```

- [ ] **Step 4: Update UpdateListingCommand.cs**

Remove `Power`, `EngineSize`, `Year` — they now live on Variant and are not directly updatable via a listing update.

```csharp
// Automotive.Marketplace.Application/Features/ListingFeatures/UpdateListing/UpdateListingCommand.cs
using MediatR;

namespace Automotive.Marketplace.Application.Features.ListingFeatures.UpdateListing;

public sealed record UpdateListingCommand : IRequest
{
    public Guid Id { get; set; }

    public decimal Price { get; set; }

    public string? Description { get; set; }

    public string? Colour { get; set; }

    public string? Vin { get; set; }

    public int Mileage { get; set; }

    public bool IsSteeringWheelRight { get; set; }

    public string City { get; set; } = string.Empty;

    public bool IsUsed { get; set; }
}
```

- [ ] **Step 5: Update UpdateListingCommandHandler.cs**

The handler maps only the fields that exist on Listing now:

```csharp
// Automotive.Marketplace.Application/Features/ListingFeatures/UpdateListing/UpdateListingCommandHandler.cs
using AutoMapper;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Domain.Entities;
using MediatR;

namespace Automotive.Marketplace.Application.Features.ListingFeatures.UpdateListing;

public class UpdateListingCommandHandler(
    IRepository repository,
    IMapper mapper) : IRequestHandler<UpdateListingCommand>
{
    public async Task Handle(UpdateListingCommand request, CancellationToken cancellationToken)
    {
        var listing = await repository.GetByIdAsync<Listing>(request.Id, cancellationToken);

        listing.Price = request.Price;
        listing.Description = request.Description;
        listing.Colour = request.Colour;
        listing.Vin = request.Vin;
        listing.Mileage = request.Mileage;
        listing.IsSteeringWheelRight = request.IsSteeringWheelRight;
        listing.City = request.City;
        listing.IsUsed = request.IsUsed;

        await repository.UpdateAsync(listing, cancellationToken);
    }
}
```

- [ ] **Step 6: Update ListingMappings.cs**

All `src.Car.*` → `src.Variant.*`. `src.Power`/`src.EngineSize` → `src.Variant.PowerKw`/`src.Variant.EngineSizeMl`. Remove `CreateListingCommand → Car` map (no longer needed — handler creates Variant directly). Remove `CreateListingCommand → Listing` map (handler creates Listing directly without AutoMapper). Remove `UpdateListingCommand → Listing` map (handler patches directly). Keep only the query-response maps.

```csharp
// Automotive.Marketplace.Application/Mappings/ListingMappings.cs
using AutoMapper;
using Automotive.Marketplace.Application.Features.ListingFeatures.GetAllListings;
using Automotive.Marketplace.Application.Features.ListingFeatures.GetListingById;
using Automotive.Marketplace.Domain.Entities;

namespace Automotive.Marketplace.Application.Mappings;

public class ListingMapping : Profile
{
    public ListingMapping()
    {
        CreateMap<Listing, GetAllListingsResponse>()
            .ForMember(dest => dest.Mileage, opt => opt.MapFrom(src => src.Mileage))
            .ForMember(dest => dest.PowerKw, opt => opt.MapFrom(src => src.Variant.PowerKw))
            .ForMember(dest => dest.EngineSizeMl, opt => opt.MapFrom(src => src.Variant.EngineSizeMl))
            .ForMember(dest => dest.IsUsed, opt => opt.MapFrom(src => src.IsUsed))
            .ForMember(dest => dest.FuelType, opt => opt.MapFrom(src => src.Variant.Fuel.Name))
            .ForMember(dest => dest.Transmission, opt => opt.MapFrom(src => src.Variant.Transmission.Name))
            .ForMember(dest => dest.Year, opt => opt.MapFrom(src => src.Variant.Year))
            .ForMember(dest => dest.Model, opt => opt.MapFrom(src => src.Variant.Model.Name))
            .ForMember(dest => dest.Make, opt => opt.MapFrom(src => src.Variant.Model.Make.Name))
            .ForMember(dest => dest.Images, opt => opt.Ignore());

        CreateMap<Listing, GetListingByIdResponse>()
            .ForMember(dest => dest.Make, opt => opt.MapFrom(src => src.Variant.Model.Make.Name))
            .ForMember(dest => dest.Model, opt => opt.MapFrom(src => src.Variant.Model.Name))
            .ForMember(dest => dest.Price, opt => opt.MapFrom(src => src.Price))
            .ForMember(dest => dest.Description, opt => opt.MapFrom(src => src.Description))
            .ForMember(dest => dest.Colour, opt => opt.MapFrom(src => src.Colour))
            .ForMember(dest => dest.Vin, opt => opt.MapFrom(src => src.Vin))
            .ForMember(dest => dest.PowerKw, opt => opt.MapFrom(src => src.Variant.PowerKw))
            .ForMember(dest => dest.EngineSizeMl, opt => opt.MapFrom(src => src.Variant.EngineSizeMl))
            .ForMember(dest => dest.Mileage, opt => opt.MapFrom(src => src.Mileage))
            .ForMember(dest => dest.IsSteeringWheelRight, opt => opt.MapFrom(src => src.IsSteeringWheelRight))
            .ForMember(dest => dest.City, opt => opt.MapFrom(src => src.City))
            .ForMember(dest => dest.IsUsed, opt => opt.MapFrom(src => src.IsUsed))
            .ForMember(dest => dest.Year, opt => opt.MapFrom(src => src.Variant.Year))
            .ForMember(dest => dest.Transmission, opt => opt.MapFrom(src => src.Variant.Transmission.Name))
            .ForMember(dest => dest.Fuel, opt => opt.MapFrom(src => src.Variant.Fuel.Name))
            .ForMember(dest => dest.DoorCount, opt => opt.MapFrom(src => src.Variant.DoorCount))
            .ForMember(dest => dest.BodyType, opt => opt.MapFrom(src => src.Variant.BodyType.Name))
            .ForMember(dest => dest.Drivetrain, opt => opt.MapFrom(src => src.Drivetrain.Name))
            .ForMember(dest => dest.Seller, opt => opt.MapFrom(src => src.Seller.Username))
            .ForMember(dest => dest.SellerId, opt => opt.MapFrom(src => src.SellerId))
            .ForMember(dest => dest.Images, opt => opt.Ignore());
    }
}
```

- [ ] **Step 7: Build the solution**

```bash
dotnet build Automotive.Marketplace.sln
```
Expected: Build succeeded, 0 errors.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat: application — VariantFeatures, enum DB queries, updated listing handlers

- Add VariantFeatures: CreateVariant, DeleteVariant, GetAllVariants, GetVariantById, UpdateVariant, GetVariantsByModel
- Delete CarFeatures, CarMappings, CarController
- Add VariantController with Permission.ViewVariants / CreateVariants / ManageVariants
- Enum handlers query DB (IRepository) instead of Enum.GetValues; responses include Id
- CreateListingCommand: VariantId-based or full-spec find-or-create; DrivetrainId replaces Drivetrain enum
- GetAllListings/GetListingById: Power→PowerKw, EngineSize→EngineSizeMl, read from Variant
- UpdateListingCommand: remove Power, EngineSize, Year (now on Variant)
- Permission enum: ViewCars→ViewVariants, CreateCars→CreateVariants, ManageCars→ManageVariants

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
```

---

## Task 13: Tests — Update CreateListingCommandHandlerTests

**Files:**
- Modify: `Automotive.Marketplace.Tests/Features/HandlerTests/ListingHandlerTests/CreateListingCommandHandlerTests.cs`

The existing tests use the old command shape (enum Fuel/Transmission/BodyType/Drivetrain, Car entity). Replace them with tests for the new shape. Three test cases: (1) provide VariantId directly, (2) full-spec with IsCustom=false creates new Variant when no match exists, (3) full-spec with IsCustom=false reuses an existing matching Variant.

- [ ] **Step 1: Write the failing tests**

Replace the entire file:

```csharp
// Automotive.Marketplace.Tests/Features/HandlerTests/ListingHandlerTests/CreateListingCommandHandlerTests.cs
using Automotive.Marketplace.Application.Features.ListingFeatures.CreateListing;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Application.Interfaces.Services;
using Automotive.Marketplace.Application.Models;
using Automotive.Marketplace.Domain.Entities;
using Automotive.Marketplace.Infrastructure.Data.Builders;
using Automotive.Marketplace.Infrastructure.Data.DatabaseContext;
using Automotive.Marketplace.Tests.Infrastructure;
using FluentAssertions;
using Microsoft.AspNetCore.Http;
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.DependencyInjection;
using NSubstitute;
using ModelBuilder = Automotive.Marketplace.Infrastructure.Data.Builders.ModelBuilder;

namespace Automotive.Marketplace.Tests.Features.HandlerTests.ListingHandlerTests;

public class CreateListingCommandHandlerTests(
    DatabaseFixture<CreateListingCommandHandlerTests> fixture)
    : IClassFixture<DatabaseFixture<CreateListingCommandHandlerTests>>, IAsyncLifetime
{
    private readonly DatabaseFixture<CreateListingCommandHandlerTests> _fixture = fixture;
    private readonly IImageStorageService _imageStorageService = Substitute.For<IImageStorageService>();

    public Task InitializeAsync() => Task.CompletedTask;

    public async Task DisposeAsync() => await _fixture.ResetDatabaseAsync();

    private CreateListingCommandHandler CreateHandler(IServiceScope scope)
    {
        var repository = scope.ServiceProvider.GetRequiredService<IRepository>();
        return new CreateListingCommandHandler(repository, _imageStorageService);
    }

    private async Task<(Make make, Model model, Fuel fuel, Transmission transmission, BodyType bodyType, Drivetrain drivetrain, User user)>
        SeedReferenceDataAsync(AutomotiveContext context)
    {
        var make = new MakeBuilder().Build();
        var model = new ModelBuilder().WithMake(make.Id).Build();
        var fuel = new Fuel { Id = Guid.NewGuid(), Name = "Petrol" };
        var transmission = new Transmission { Id = Guid.NewGuid(), Name = "Manual" };
        var bodyType = new BodyType { Id = Guid.NewGuid(), Name = "Sedan" };
        var drivetrain = new Drivetrain { Id = Guid.NewGuid(), Name = "FWD" };
        var user = new UserBuilder().Build();

        await context.AddRangeAsync(make, model, fuel, transmission, bodyType, drivetrain, user);
        await context.SaveChangesAsync();

        return (make, model, fuel, transmission, bodyType, drivetrain, user);
    }

    [Fact]
    public async Task Handle_WithVariantId_UsesExistingVariant()
    {
        await using var scope = _fixture.ServiceProvider.CreateAsyncScope();
        var context = scope.ServiceProvider.GetRequiredService<AutomotiveContext>();
        var handler = CreateHandler(scope);

        var (make, model, fuel, transmission, bodyType, drivetrain, user) =
            await SeedReferenceDataAsync(context);

        var variant = new VariantBuilder()
            .WithModel(model.Id)
            .WithFuel(fuel.Id)
            .WithTransmission(transmission.Id)
            .WithBodyType(bodyType.Id)
            .Build();
        await context.AddAsync(variant);
        await context.SaveChangesAsync();

        _imageStorageService.UploadImageAsync(Arg.Any<IFormFile>(), Arg.Any<string>())
            .Returns(new ImageUploadResult { BucketName = "b", ObjectKey = "k", FileSize = 1 });

        var mockImage = Substitute.For<IFormFile>();
        mockImage.FileName.Returns("img.jpg");
        mockImage.ContentType.Returns("image/jpeg");

        var command = new CreateListingCommand
        {
            VariantId = variant.Id,
            DrivetrainId = drivetrain.Id,
            Price = 5000,
            Mileage = 100000,
            City = "Vilnius",
            IsUsed = true,
            IsSteeringWheelRight = false,
            UserId = user.Id,
            Images = [mockImage],
        };

        await handler.Handle(command, CancellationToken.None);

        var listings = await context.Set<Listing>().ToListAsync();
        listings.Count.Should().Be(1);
        listings[0].VariantId.Should().Be(variant.Id);
        listings[0].DrivetrainId.Should().Be(drivetrain.Id);

        var variantCount = await context.Set<Variant>().CountAsync();
        variantCount.Should().Be(1);
    }

    [Fact]
    public async Task Handle_WithFullSpec_IsCustomFalse_CreatesNewVariantWhenNoMatch()
    {
        await using var scope = _fixture.ServiceProvider.CreateAsyncScope();
        var context = scope.ServiceProvider.GetRequiredService<AutomotiveContext>();
        var handler = CreateHandler(scope);

        var (_, model, fuel, transmission, bodyType, drivetrain, user) =
            await SeedReferenceDataAsync(context);

        _imageStorageService.UploadImageAsync(Arg.Any<IFormFile>(), Arg.Any<string>())
            .Returns(new ImageUploadResult { BucketName = "b", ObjectKey = "k", FileSize = 1 });

        var mockImage = Substitute.For<IFormFile>();
        mockImage.FileName.Returns("img.jpg");
        mockImage.ContentType.Returns("image/jpeg");

        var command = new CreateListingCommand
        {
            ModelId = model.Id,
            FuelId = fuel.Id,
            TransmissionId = transmission.Id,
            BodyTypeId = bodyType.Id,
            DoorCount = 4,
            PowerKw = 110,
            EngineSizeMl = 2000,
            Year = 2020,
            IsCustom = false,
            DrivetrainId = drivetrain.Id,
            Price = 15000,
            Mileage = 50000,
            City = "Kaunas",
            IsUsed = true,
            IsSteeringWheelRight = false,
            UserId = user.Id,
            Images = [mockImage],
        };

        await handler.Handle(command, CancellationToken.None);

        var variants = await context.Set<Variant>().ToListAsync();
        var listings = await context.Set<Listing>().ToListAsync();

        variants.Count.Should().Be(1);
        listings.Count.Should().Be(1);
        listings[0].VariantId.Should().Be(variants[0].Id);
        variants[0].Year.Should().Be(2020);
        variants[0].PowerKw.Should().Be(110);
        variants[0].IsCustom.Should().BeFalse();
    }

    [Fact]
    public async Task Handle_WithFullSpec_IsCustomFalse_ReusesExistingMatchingVariant()
    {
        await using var scope = _fixture.ServiceProvider.CreateAsyncScope();
        var context = scope.ServiceProvider.GetRequiredService<AutomotiveContext>();
        var handler = CreateHandler(scope);

        var (_, model, fuel, transmission, bodyType, drivetrain, user) =
            await SeedReferenceDataAsync(context);

        var existingVariant = new VariantBuilder()
            .WithModel(model.Id)
            .WithFuel(fuel.Id)
            .WithTransmission(transmission.Id)
            .WithBodyType(bodyType.Id)
            .With(v => v.DoorCount, 4)
            .With(v => v.PowerKw, 110)
            .With(v => v.EngineSizeMl, 2000)
            .With(v => v.Year, 2020)
            .With(v => v.IsCustom, false)
            .Build();
        await context.AddAsync(existingVariant);
        await context.SaveChangesAsync();

        _imageStorageService.UploadImageAsync(Arg.Any<IFormFile>(), Arg.Any<string>())
            .Returns(new ImageUploadResult { BucketName = "b", ObjectKey = "k", FileSize = 1 });

        var mockImage = Substitute.For<IFormFile>();
        mockImage.FileName.Returns("img.jpg");
        mockImage.ContentType.Returns("image/jpeg");

        var command = new CreateListingCommand
        {
            ModelId = model.Id,
            FuelId = fuel.Id,
            TransmissionId = transmission.Id,
            BodyTypeId = bodyType.Id,
            DoorCount = 4,
            PowerKw = 110,
            EngineSizeMl = 2000,
            Year = 2020,
            IsCustom = false,
            DrivetrainId = drivetrain.Id,
            Price = 9000,
            Mileage = 30000,
            City = "Klaipeda",
            IsUsed = true,
            IsSteeringWheelRight = false,
            UserId = user.Id,
            Images = [mockImage],
        };

        await handler.Handle(command, CancellationToken.None);

        var variantCount = await context.Set<Variant>().CountAsync();
        var listings = await context.Set<Listing>().ToListAsync();

        variantCount.Should().Be(1);
        listings.Count.Should().Be(1);
        listings[0].VariantId.Should().Be(existingVariant.Id);
    }

    [Fact]
    public async Task Handle_WithFullSpec_IsCustomTrue_AlwaysCreatesNewVariant()
    {
        await using var scope = _fixture.ServiceProvider.CreateAsyncScope();
        var context = scope.ServiceProvider.GetRequiredService<AutomotiveContext>();
        var handler = CreateHandler(scope);

        var (_, model, fuel, transmission, bodyType, drivetrain, user) =
            await SeedReferenceDataAsync(context);

        var existingVariant = new VariantBuilder()
            .WithModel(model.Id)
            .WithFuel(fuel.Id)
            .WithTransmission(transmission.Id)
            .WithBodyType(bodyType.Id)
            .With(v => v.DoorCount, 4)
            .With(v => v.PowerKw, 110)
            .With(v => v.EngineSizeMl, 2000)
            .With(v => v.Year, 2020)
            .With(v => v.IsCustom, false)
            .Build();
        await context.AddAsync(existingVariant);
        await context.SaveChangesAsync();

        _imageStorageService.UploadImageAsync(Arg.Any<IFormFile>(), Arg.Any<string>())
            .Returns(new ImageUploadResult { BucketName = "b", ObjectKey = "k", FileSize = 1 });

        var mockImage = Substitute.For<IFormFile>();
        mockImage.FileName.Returns("img.jpg");
        mockImage.ContentType.Returns("image/jpeg");

        var command = new CreateListingCommand
        {
            ModelId = model.Id,
            FuelId = fuel.Id,
            TransmissionId = transmission.Id,
            BodyTypeId = bodyType.Id,
            DoorCount = 4,
            PowerKw = 110,
            EngineSizeMl = 2000,
            Year = 2020,
            IsCustom = true,
            DrivetrainId = drivetrain.Id,
            Price = 7000,
            Mileage = 80000,
            City = "Siauliai",
            IsUsed = true,
            IsSteeringWheelRight = false,
            UserId = user.Id,
            Images = [mockImage],
        };

        await handler.Handle(command, CancellationToken.None);

        var variantCount = await context.Set<Variant>().CountAsync();
        variantCount.Should().Be(2);

        var newVariant = await context.Set<Variant>().FirstAsync(v => v.IsCustom);
        newVariant.IsCustom.Should().BeTrue();
    }
}
```

- [ ] **Step 2: Run tests to confirm they fail (before Task 11 is implemented)**

```bash
dotnet test Automotive.Marketplace.Tests   --filter "ClassName=Automotive.Marketplace.Tests.Features.HandlerTests.ListingHandlerTests.CreateListingCommandHandlerTests"
```
Expected: Compilation errors (old command shape referenced) or test failures.

- [ ] **Step 3: Complete Task 11 (implement the handler)**

Go back and implement CreateListingCommand.cs and CreateListingCommandHandler.cs (Task 11).

- [ ] **Step 4: Run tests again — should pass**

```bash
dotnet test Automotive.Marketplace.Tests   --filter "ClassName=Automotive.Marketplace.Tests.Features.HandlerTests.ListingHandlerTests.CreateListingCommandHandlerTests"
```
Expected: 4 tests pass.

---

## Task 14: Tests — Update GetAllListingsQueryHandlerTests

**Files:**
- Modify: `Automotive.Marketplace.Tests/Features/HandlerTests/ListingHandlerTests/GetAllListingsQueryHandlerTests.cs`

- [ ] **Step 1: Update the SeedListingsAsync helper and all tests**

The helper uses `CarBuilder` and `ListingBuilder.WithCar`. Replace with `VariantBuilder` and `ListingBuilder.WithVariant`. The `carYear` parameter (DateTime) becomes `variantYear` (int). Power filter was on Listing, now on Variant.

```csharp
// Automotive.Marketplace.Tests/Features/HandlerTests/ListingHandlerTests/GetAllListingsQueryHandlerTests.cs
using Automotive.Marketplace.Application.Features.ListingFeatures.GetAllListings;
using Automotive.Marketplace.Application.Interfaces.Data;
using Automotive.Marketplace.Application.Interfaces.Services;
using Automotive.Marketplace.Domain.Entities;
using Automotive.Marketplace.Infrastructure.Data;
using Automotive.Marketplace.Infrastructure.Data.Builders;
using Automotive.Marketplace.Infrastructure.Data.DatabaseContext;
using Automotive.Marketplace.Tests.Infrastructure;
using FluentAssertions;
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.DependencyInjection;
using NSubstitute;
using ModelBuilder = Automotive.Marketplace.Infrastructure.Data.Builders.ModelBuilder;

namespace Automotive.Marketplace.Tests.Features.HandlerTests.ListingHandlerTests;

public class GetAllListingsQueryHandlerTests(
    DatabaseFixture<GetAllListingsQueryHandlerTests> fixture)
    : IClassFixture<DatabaseFixture<GetAllListingsQueryHandlerTests>>, IAsyncLifetime
{
    private readonly DatabaseFixture<GetAllListingsQueryHandlerTests> _fixture = fixture;
    private readonly IImageStorageService _imageStorageService = Substitute.For<IImageStorageService>();

    public Task InitializeAsync() => Task.CompletedTask;

    public async Task DisposeAsync() => await _fixture.ResetDatabaseAsync();

    private GetAllListingsQueryHandler CreateHandler(IServiceScope scope)
    {
        var mapper = scope.ServiceProvider.GetRequiredService<AutoMapper.IMapper>();
        var repository = scope.ServiceProvider.GetRequiredService<IRepository>();
        return new GetAllListingsQueryHandler(mapper, repository, _imageStorageService);
    }

    [Fact]
    public async Task Handle_NoFilter_ReturnsAllListings()
    {
        await using var scope = _fixture.ServiceProvider.CreateAsyncScope();
        var handler = CreateHandler(scope);
        var context = scope.ServiceProvider.GetRequiredService<AutomotiveContext>();

        await SeedListingsAsync(context, 3);
        await SeedListingsAsync(context, 2);

        var result = await handler.Handle(new GetAllListingsQuery(), CancellationToken.None);

        result.Count().Should().Be(5);
    }

    [Fact]
    public async Task Handle_FilterByMinYear_ShouldReturnFilteredListings()
    {
        await using var scope = _fixture.ServiceProvider.CreateAsyncScope();
        var handler = CreateHandler(scope);
        var context = scope.ServiceProvider.GetRequiredService<AutomotiveContext>();

        await SeedListingsAsync(context, 3, variantYear: 2020);
        await SeedListingsAsync(context, 2, variantYear: 2015);

        var result = await handler.Handle(new GetAllListingsQuery { MinYear = 2020 }, CancellationToken.None);

        result.Count().Should().Be(3);
    }

    [Fact]
    public async Task Handle_FilterByMaxYear_ShouldReturnFilteredListings()
    {
        await using var scope = _fixture.ServiceProvider.CreateAsyncScope();
        var handler = CreateHandler(scope);
        var context = scope.ServiceProvider.GetRequiredService<AutomotiveContext>();

        await SeedListingsAsync(context, 3, variantYear: 2000);
        await SeedListingsAsync(context, 2, variantYear: 2005);

        var result = await handler.Handle(new GetAllListingsQuery { MaxYear = 2000 }, CancellationToken.None);

        result.Count().Should().Be(3);
    }

    [Fact]
    public async Task Handle_FilterByMinPrice_ShouldReturnFilteredListings()
    {
        await using var scope = _fixture.ServiceProvider.CreateAsyncScope();
        var handler = CreateHandler(scope);
        var context = scope.ServiceProvider.GetRequiredService<AutomotiveContext>();

        await SeedListingsAsync(context, 3, price: 999);
        await SeedListingsAsync(context, 2, price: 111);

        var result = await handler.Handle(new GetAllListingsQuery { MinPrice = 999 }, CancellationToken.None);

        result.Count().Should().Be(3);
    }

    [Fact]
    public async Task Handle_FilterByMaxPrice_ShouldReturnFilteredListings()
    {
        await using var scope = _fixture.ServiceProvider.CreateAsyncScope();
        var handler = CreateHandler(scope);
        var context = scope.ServiceProvider.GetRequiredService<AutomotiveContext>();

        await SeedListingsAsync(context, 3, price: 111);
        await SeedListingsAsync(context, 2, price: 999);

        var result = await handler.Handle(new GetAllListingsQuery { MaxPrice = 111 }, CancellationToken.None);

        result.Count().Should().Be(3);
    }

    [Fact]
    public async Task Handle_FilterByMakeIdAndIsUsed_ShouldReturnFilteredListings()
    {
        await using var scope = _fixture.ServiceProvider.CreateAsyncScope();
        var handler = CreateHandler(scope);
        var context = scope.ServiceProvider.GetRequiredService<AutomotiveContext>();

        var matchingListings = await SeedListingsAsync(context, 3, isUsed: true);
        await SeedListingsAsync(context, 2, isUsed: false);

        var makeId = (await context.Set<Variant>()
            .Include(v => v.Model)
            .FirstAsync(v => v.Id == matchingListings[0].VariantId))
            .Model.MakeId;

        var result = await handler.Handle(
            new GetAllListingsQuery { MakeId = makeId, IsUsed = true },
            CancellationToken.None);

        result.Count().Should().Be(3);
    }

    private static async Task<List<Listing>> SeedListingsAsync(
        AutomotiveContext context,
        int count,
        bool? isUsed = null,
        int? variantYear = null,
        decimal? price = null)
    {
        var seller = new UserBuilder().Build();
        var make = new MakeBuilder().Build();
        var model = new ModelBuilder().WithMake(make.Id).Build();
        var fuel = new Fuel { Id = Guid.NewGuid(), Name = "Petrol" };
        var transmission = new Transmission { Id = Guid.NewGuid(), Name = "Manual" };
        var bodyType = new BodyType { Id = Guid.NewGuid(), Name = "Sedan" };
        var drivetrain = new Drivetrain { Id = Guid.NewGuid(), Name = "FWD" };

        await context.AddRangeAsync(seller, make, model, fuel, transmission, bodyType, drivetrain);
        await context.SaveChangesAsync();

        List<Listing> listings = [];
        for (int i = 0; i < count; i++)
        {
            var variantBuilder = new VariantBuilder()
                .WithModel(model.Id)
                .WithFuel(fuel.Id)
                .WithTransmission(transmission.Id)
                .WithBodyType(bodyType.Id);

            if (variantYear.HasValue)
                variantBuilder.With(v => v.Year, variantYear.Value);

            var variant = variantBuilder.Build();
            await context.AddAsync(variant);
            await context.SaveChangesAsync();

            var listingBuilder = new ListingBuilder()
                .WithSeller(seller.Id)
                .WithVariant(variant.Id)
                .WithDrivetrain(drivetrain.Id);

            if (isUsed.HasValue)
                listingBuilder.WithUsed(isUsed.Value);

            if (price.HasValue)
                listingBuilder.WithPrice(price.Value);

            listings.Add(listingBuilder.Build());
        }

        await context.AddRangeAsync(listings);
        await context.SaveChangesAsync();

        return listings;
    }
}
```

- [ ] **Step 2: Run all tests**

```bash
dotnet test Automotive.Marketplace.Tests
```
Expected: All tests pass.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "test: update listing handler tests for Variant/Drivetrain entity shape

- CreateListingCommandHandlerTests: new tests for VariantId path, full-spec path (find-or-create), IsCustom=true path
- GetAllListingsQueryHandlerTests: seed with VariantBuilder; Year filter uses int, not DateTime

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
```

---

## Task 15: Frontend — Update Enum Types + Toggle/Select Component Values

**Files:**
- Modify: `automotive.marketplace.client/src/types/enum/GetFuelTypesResponse.ts`
- Modify: `automotive.marketplace.client/src/types/enum/GetBodyTypesResponse.ts`
- Modify: `automotive.marketplace.client/src/types/enum/GetTransmissionTypesResponse.ts`
- Modify: `automotive.marketplace.client/src/types/enum/GetDrivetrainTypesResponse.ts`
- Modify: `automotive.marketplace.client/src/components/forms/DrivetrainToggleGroup.tsx`
- Modify: `automotive.marketplace.client/src/components/forms/TransmissionToggleGroup.tsx`
- Modify: `automotive.marketplace.client/src/components/forms/select/FuelSelect.tsx`
- Modify: `automotive.marketplace.client/src/components/forms/select/BodyTypeSelect.tsx`

The API now returns `{ id, fuelType }` instead of `{ fuelType }`. The toggle/select components must use `id` as the form value (so the backend receives a GUID).

- [ ] **Step 1: Update enum response types**

```typescript
// automotive.marketplace.client/src/types/enum/GetFuelTypesResponse.ts
export type GetFuelTypesResponse = {
  id: string;
  fuelType: string;
};
```

```typescript
// automotive.marketplace.client/src/types/enum/GetBodyTypesResponse.ts
export type GetBodyTypesResponse = {
  id: string;
  bodyType: string;
};
```

```typescript
// automotive.marketplace.client/src/types/enum/GetTransmissionTypesResponse.ts
export type GetTransmissionTypesResponse = {
  id: string;
  transmissionType: string;
};
```

```typescript
// automotive.marketplace.client/src/types/enum/GetDrivetrainTypesResponse.ts
export type GetDrivetrainTypesResponse = {
  id: string;
  drivetrainType: string;
};
```

- [ ] **Step 2: Update DrivetrainToggleGroup.tsx — use id as value**

```tsx
// automotive.marketplace.client/src/components/forms/DrivetrainToggleGroup.tsx
import { getDrivetrainTypesOptions } from "@/api/enum/getDrivetrainTypesOptions";
import { toggleVariants } from "@/components/ui/toggle";
import { cn } from "@/lib/utils";
import * as ToggleGroupPrimitive from "@radix-ui/react-toggle-group";
import { useQuery } from "@tanstack/react-query";
import { type VariantProps } from "class-variance-authority";
import { ToggleGroup, ToggleGroupItem } from "../ui/toggle-group";

type DrivetrainToggleGroupProps = React.ComponentProps<
  typeof ToggleGroupPrimitive.Root
> &
  VariantProps<typeof toggleVariants>;

const DrivetrainToggleGroup = ({
  className,
  ...props
}: DrivetrainToggleGroupProps) => {
  const { data: drivetrainTypesQuery } = useQuery(getDrivetrainTypesOptions);
  const drivetrainTypes = drivetrainTypesQuery?.data || [];

  return (
    <div className={cn(className)}>
      <ToggleGroup {...props}>
        {drivetrainTypes.map((drivetrain) => (
          <ToggleGroupItem key={drivetrain.id} value={drivetrain.id}>
            {drivetrain.drivetrainType}
          </ToggleGroupItem>
        ))}
      </ToggleGroup>
    </div>
  );
};

export default DrivetrainToggleGroup;
```

- [ ] **Step 3: Update TransmissionToggleGroup.tsx — use id as value**

```tsx
// automotive.marketplace.client/src/components/forms/TransmissionToggleGroup.tsx
import { getTransmissionTypesOptions } from "@/api/enum/getTransmissionTypesOptions";
import { toggleVariants } from "@/components/ui/toggle";
import { cn } from "@/lib/utils";
import * as ToggleGroupPrimitive from "@radix-ui/react-toggle-group";
import { useQuery } from "@tanstack/react-query";
import { type VariantProps } from "class-variance-authority";
import { ToggleGroup, ToggleGroupItem } from "../ui/toggle-group";

type TransmissionToggleGroupProps = React.ComponentProps<
  typeof ToggleGroupPrimitive.Root
> &
  VariantProps<typeof toggleVariants>;

const TransmissionToggleGroup = ({
  className,
  ...props
}: TransmissionToggleGroupProps) => {
  const { data: transmissionTypesQuery } = useQuery(getTransmissionTypesOptions);
  const transmissionTypes = transmissionTypesQuery?.data || [];

  return (
    <div className={cn(className)}>
      <ToggleGroup {...props}>
        {transmissionTypes.map((transmission) => (
          <ToggleGroupItem
            key={transmission.id}
            value={transmission.id}
            className="flex-none"
          >
            {transmission.transmissionType}
          </ToggleGroupItem>
        ))}
      </ToggleGroup>
    </div>
  );
};

export default TransmissionToggleGroup;
```

- [ ] **Step 4: Check FuelSelect and BodyTypeSelect**

Open these files and confirm they use `drivetrainType`/`fuelType` as option values. If so, update them to use `id` as option value (same pattern as the toggles). The display label remains the human-readable name. The exact implementation depends on how `BasicSelect` is wired — but the principle is: `value={item.id}` and `label={item.fuelType}`.

---

## Task 16: Frontend — variantList Feature (Replaces carList)

**Files:**
- Delete: `automotive.marketplace.client/src/features/carList/` (entire folder)
- Delete: `automotive.marketplace.client/src/api/queryKeys/carKeys.ts`
- Create: `automotive.marketplace.client/src/api/queryKeys/variantKeys.ts`
- Create: `automotive.marketplace.client/src/features/variantList/` (full folder below)

- [ ] **Step 1: Delete carList folder and carKeys**

```bash
rm -rf automotive.marketplace.client/src/features/carList
rm automotive.marketplace.client/src/api/queryKeys/carKeys.ts
```

- [ ] **Step 2: Create variantKeys.ts**

```typescript
// automotive.marketplace.client/src/api/queryKeys/variantKeys.ts
export const variantKeys = {
  all: () => ["variant"],
  byModel: (modelId: string, year?: number) => [...variantKeys.all(), "byModel", modelId, year],
  byId: (id: string) => [...variantKeys.all(), id],
};
```

- [ ] **Step 3: Update ENDPOINTS constant — add VARIANT block**

```typescript
// automotive.marketplace.client/src/constants/endpoints.ts (add inside ENDPOINTS object)
VARIANT: {
  GET_BY_ID: "/Variant/GetById",
  GET_ALL: "/Variant/GetAll",
  GET_BY_MODEL: "/Variant/GetByModel",
  CREATE: "/Variant/Create",
  DELETE: "/Variant/Delete",
  UPDATE: "/Variant/Update",
},
```

Remove the `CAR` block from `ENDPOINTS` (it's no longer needed).

- [ ] **Step 4: Create type files**

```typescript
// automotive.marketplace.client/src/features/variantList/types/GetAllVariantsResponse.ts
export type GetAllVariantsResponse = {
  id: string;
  year: number;
  make: string;
  model: string;
  fuelType: string;
  transmission: string;
};
```

```typescript
// automotive.marketplace.client/src/features/variantList/types/GetVariantByIdResponse.ts
export type GetVariantByIdResponse = {
  id: string;
  year: number;
  fuelId: string;
  fuel: string;
  transmissionId: string;
  transmission: string;
  bodyTypeId: string;
  bodyType: string;
  doorCount: number;
  powerKw: number;
  engineSizeMl: number;
  isCustom: boolean;
  makeId: string;
  modelId: string;
};
```

```typescript
// automotive.marketplace.client/src/features/variantList/types/GetVariantsByModelResponse.ts
export type GetVariantsByModelResponse = {
  id: string;
  year: number;
  fuelType: string;
  transmission: string;
  doorCount: number;
  powerKw: number;
  engineSizeMl: number;
};
```

```typescript
// automotive.marketplace.client/src/features/variantList/types/CreateVariantCommand.ts
export type CreateVariantCommand = {
  year: number;
  fuelId: string;
  transmissionId: string;
  bodyTypeId: string;
  doorCount: number;
  powerKw: number;
  engineSizeMl: number;
  isCustom: boolean;
  modelId: string;
};
```

```typescript
// automotive.marketplace.client/src/features/variantList/types/UpdateVariantCommand.ts
export type UpdateVariantCommand = {
  id: string;
  year: number;
  fuelId: string;
  transmissionId: string;
  bodyTypeId: string;
  doorCount: number;
  powerKw: number;
  engineSizeMl: number;
  isCustom: boolean;
  modelId: string;
};
```

```typescript
// automotive.marketplace.client/src/features/variantList/types/DeleteVariantCommand.ts
export type DeleteVariantCommand = {
  id: string;
};
```

```typescript
// automotive.marketplace.client/src/features/variantList/types/GetVariantByIdQuery.ts
export type GetVariantByIdQuery = {
  id: string;
};
```

- [ ] **Step 5: Create variantFormSchema.ts**

```typescript
// automotive.marketplace.client/src/features/variantList/schemas/variantFormSchema.ts
import { VALIDATION } from "@/constants/validation";
import { validation } from "@/utils/validation";
import z from "zod";

export const VariantFormSchema = z.object({
  year: z.coerce
    .number<number>()
    .min(VALIDATION.YEAR.MIN, {
      error: validation.minSize({ label: "Year", size: VALIDATION.YEAR.MIN }),
    })
    .max(new Date().getFullYear(), {
      error: validation.maxSize({ label: "Year", size: new Date().getFullYear() }),
    }),
  transmissionId: z.string().nonempty({ error: "Please select a transmission type" }),
  fuelId: z.string().nonempty({ error: "Please select a fuel type" }),
  bodyTypeId: z.string().nonempty({ error: "Please select a body type" }),
  doorCount: z.coerce
    .number<number>()
    .min(VALIDATION.DOOR_COUNT.MIN, {
      error: validation.minSize({ label: "Door count", size: VALIDATION.DOOR_COUNT.MIN }),
    })
    .max(VALIDATION.DOOR_COUNT.MAX, {
      error: validation.maxSize({ label: "Door count", size: VALIDATION.DOOR_COUNT.MAX }),
    }),
  powerKw: z.coerce
    .number<number>()
    .int()
    .min(VALIDATION.POWER.MIN, {
      error: validation.minSize({ label: "Power", size: VALIDATION.POWER.MIN, unit: "kW" }),
    })
    .max(VALIDATION.POWER.MAX, {
      error: validation.maxSize({ label: "Power", size: VALIDATION.POWER.MAX, unit: "kW" }),
    }),
  engineSizeMl: z.coerce
    .number<number>()
    .int()
    .min(VALIDATION.ENGINE_SIZE.MIN, {
      error: validation.minSize({ label: "Engine size", size: VALIDATION.ENGINE_SIZE.MIN, unit: "ml" }),
    })
    .max(VALIDATION.ENGINE_SIZE.MAX, {
      error: validation.maxSize({ label: "Engine size", size: VALIDATION.ENGINE_SIZE.MAX, unit: "ml" }),
    }),
  isCustom: z.boolean(),
  makeId: z.string().regex(VALIDATION.GUID.REGEX, { error: "Please select a make" }),
  modelId: z.string().regex(VALIDATION.GUID.REGEX, { error: "Please select a model" }),
});
```

```typescript
// automotive.marketplace.client/src/features/variantList/types/VariantFormData.ts
import z from "zod";
import { VariantFormSchema } from "../schemas/variantFormSchema";

export type VariantFormData = z.infer<typeof VariantFormSchema>;
```

- [ ] **Step 6: Create API option files**

```typescript
// automotive.marketplace.client/src/features/variantList/api/getAllVariantsOptions.ts
import { variantKeys } from "@/api/queryKeys/variantKeys";
import { ENDPOINTS } from "@/constants/endpoints";
import axiosClient from "@/lib/axios/axiosClient";
import { queryOptions } from "@tanstack/react-query";
import { GetAllVariantsResponse } from "../types/GetAllVariantsResponse";

const getAllVariants = () =>
  axiosClient.get<GetAllVariantsResponse[]>(ENDPOINTS.VARIANT.GET_ALL);

export const getAllVariantsOptions = queryOptions({
  queryKey: variantKeys.all(),
  queryFn: getAllVariants,
});
```

```typescript
// automotive.marketplace.client/src/features/variantList/api/getVariantByIdOptions.ts
import { variantKeys } from "@/api/queryKeys/variantKeys";
import { ENDPOINTS } from "@/constants/endpoints";
import axiosClient from "@/lib/axios/axiosClient";
import { queryOptions } from "@tanstack/react-query";
import { GetVariantByIdQuery } from "../types/GetVariantByIdQuery";
import { GetVariantByIdResponse } from "../types/GetVariantByIdResponse";

const getVariantById = (query: GetVariantByIdQuery) =>
  axiosClient.get<GetVariantByIdResponse>(ENDPOINTS.VARIANT.GET_BY_ID, { params: query });

export const getVariantByIdOptions = (query: GetVariantByIdQuery) =>
  queryOptions({
    queryKey: variantKeys.byId(query.id),
    queryFn: () => getVariantById(query),
  });
```

```typescript
// automotive.marketplace.client/src/features/variantList/api/getVariantsByModelOptions.ts
import { variantKeys } from "@/api/queryKeys/variantKeys";
import { ENDPOINTS } from "@/constants/endpoints";
import axiosClient from "@/lib/axios/axiosClient";
import { queryOptions } from "@tanstack/react-query";
import { GetVariantsByModelResponse } from "../types/GetVariantsByModelResponse";

type Params = { modelId: string; year?: number };

const getVariantsByModel = (params: Params) =>
  axiosClient.get<GetVariantsByModelResponse[]>(ENDPOINTS.VARIANT.GET_BY_MODEL, { params });

export const getVariantsByModelOptions = (params: Params) =>
  queryOptions({
    queryKey: variantKeys.byModel(params.modelId, params.year),
    queryFn: () => getVariantsByModel(params),
    enabled: !!params.modelId,
  });
```

```typescript
// automotive.marketplace.client/src/features/variantList/api/useCreateVariant.ts
import { variantKeys } from "@/api/queryKeys/variantKeys";
import { ENDPOINTS } from "@/constants/endpoints";
import axiosClient from "@/lib/axios/axiosClient";
import { useMutation } from "@tanstack/react-query";
import { CreateVariantCommand } from "../types/CreateVariantCommand";

const createVariant = (body: CreateVariantCommand) =>
  axiosClient.post<void>(ENDPOINTS.VARIANT.CREATE, body);

export const useCreateVariant = () =>
  useMutation({
    mutationFn: createVariant,
    meta: {
      successMessage: "Successfully created Variant!",
      errorMessage: "Sorry, we couldn't create your Variant",
      invalidatesQuery: variantKeys.all(),
    },
  });
```

```typescript
// automotive.marketplace.client/src/features/variantList/api/useUpdateVariant.ts
import { variantKeys } from "@/api/queryKeys/variantKeys";
import { ENDPOINTS } from "@/constants/endpoints";
import axiosClient from "@/lib/axios/axiosClient";
import { useMutation } from "@tanstack/react-query";
import { UpdateVariantCommand } from "../types/UpdateVariantCommand";

const updateVariant = (body: UpdateVariantCommand) =>
  axiosClient.put<void>(ENDPOINTS.VARIANT.UPDATE, body);

export const useUpdateVariant = () =>
  useMutation({
    mutationFn: updateVariant,
    meta: {
      successMessage: "Successfully updated Variant!",
      errorMessage: "Sorry, we couldn't update Variant",
      invalidatesQuery: variantKeys.all(),
    },
  });
```

```typescript
// automotive.marketplace.client/src/features/variantList/api/useDeleteVariant.ts
import { variantKeys } from "@/api/queryKeys/variantKeys";
import { ENDPOINTS } from "@/constants/endpoints";
import axiosClient from "@/lib/axios/axiosClient";
import { useMutation } from "@tanstack/react-query";
import { DeleteVariantCommand } from "../types/DeleteVariantCommand";

const deleteVariant = (query: DeleteVariantCommand) =>
  axiosClient.delete<void>(ENDPOINTS.VARIANT.DELETE, { params: query });

export const useDeleteVariant = () =>
  useMutation({
    mutationFn: deleteVariant,
    meta: {
      successMessage: "Successfully deleted Variant!",
      errorMessage: "Sorry, we had trouble deleting your Variant",
      invalidatesQuery: variantKeys.all(),
    },
  });
```

- [ ] **Step 7: Create VariantForm.tsx**

Mirror the existing CarForm.tsx but using `fuelId`/`transmissionId`/`bodyTypeId` (GUIDs) + `powerKw`/`engineSizeMl`:

```tsx
// automotive.marketplace.client/src/features/variantList/components/VariantForm.tsx
import BodyTypeSelect from "@/components/forms/select/BodyTypeSelect";
import FuelSelect from "@/components/forms/select/FuelSelect";
import MakeSelect from "@/components/forms/select/MakeSelect";
import ModelSelect from "@/components/forms/select/ModelSelect";
import TransmissionToggleGroup from "@/components/forms/TransmissionToggleGroup";
import { Button } from "@/components/ui/button";
import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from "@/components/ui/form";
import { Input } from "@/components/ui/input";
import { cn } from "@/lib/utils";
import { zodResolver } from "@hookform/resolvers/zod";
import { useForm } from "react-hook-form";
import { VariantFormSchema } from "../schemas/variantFormSchema";
import { VariantFormData } from "../types/VariantFormData";

type VariantFormProps = {
  variant: VariantFormData;
  onSubmit: (formData: VariantFormData) => Promise<void>;
  className?: string;
};

const VariantForm = ({ variant, onSubmit, className }: VariantFormProps) => {
  const form = useForm<VariantFormData>({
    defaultValues: variant,
    resolver: zodResolver(VariantFormSchema),
  });

  const handleSubmit = async (formData: VariantFormData) => {
    await onSubmit(formData);
    form.reset();
  };

  const selectedMake = form.watch("makeId");

  return (
    <div className={cn(className)}>
      <Form {...form}>
        <form
          className="grid w-full min-w-3xs grid-cols-4 gap-x-6 gap-y-6 md:gap-x-12 md:gap-y-8"
          onSubmit={form.handleSubmit(handleSubmit)}
        >
          <FormField name="makeId" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Make*</FormLabel>
              <FormControl>
                <MakeSelect isAllMakesEnabled={false} onValueChange={field.onChange} {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="modelId" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Model*</FormLabel>
              <FormControl>
                <ModelSelect isAllModelsEnabled={false} disabled={!selectedMake} onValueChange={field.onChange} selectedMake={selectedMake} {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="year" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Year*</FormLabel>
              <FormControl><Input type="number" min={0} {...field} /></FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="transmissionId" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Transmission type*</FormLabel>
              <FormControl>
                <TransmissionToggleGroup type="single" onValueChange={field.onChange} {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="fuelId" control={form.control} render={({ field }) => (
            <FormItem className="col-span-1 flex flex-col justify-start">
              <FormLabel>Fuel type*</FormLabel>
              <FormControl>
                <FuelSelect onValueChange={field.onChange} {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="bodyTypeId" control={form.control} render={({ field }) => (
            <FormItem className="col-span-1 flex flex-col justify-start">
              <FormLabel>Body type*</FormLabel>
              <FormControl>
                <BodyTypeSelect onValueChange={field.onChange} {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="doorCount" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Door count*</FormLabel>
              <FormControl><Input type="number" {...field} /></FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="powerKw" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Power (kW)*</FormLabel>
              <FormControl><Input type="number" min={0} {...field} /></FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="engineSizeMl" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Engine size (ml)*</FormLabel>
              <FormControl><Input type="number" min={0} {...field} /></FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <Button type="submit">Confirm</Button>
        </form>
      </Form>
    </div>
  );
};

export default VariantForm;
```

- [ ] **Step 8: Create VariantListTable.tsx, CreateVariantDialog.tsx, EditVariantDialog.tsx, EditVariantDialogContent.tsx, ViewVariantDialog.tsx, ViewVariantDialogContent.tsx**

These mirror the carList equivalents. Use `useCreateVariant`, `useUpdateVariant`, `useDeleteVariant`, `getAllVariantsOptions`, `getVariantByIdOptions`. Labels say "Variant" instead of "Car". Table columns: Year, Make, Model, Fuel Type, Transmission, Actions.

```tsx
// automotive.marketplace.client/src/features/variantList/components/VariantListTable.tsx
import { Button } from "@/components/ui/button";
import { Table, TableBody, TableCaption, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { cn } from "@/lib/utils";
import { useQuery } from "@tanstack/react-query";
import { Trash } from "lucide-react";
import { getAllVariantsOptions } from "../api/getAllVariantsOptions";
import { useDeleteVariant } from "../api/useDeleteVariant";
import EditVariantDialog from "./EditVariantDialog";
import ViewVariantDialog from "./ViewVariantDialog";

type VariantListTableProps = { className?: string };

const VariantListTable = ({ className }: VariantListTableProps) => {
  const { data: variantsQuery } = useQuery(getAllVariantsOptions);
  const variants = variantsQuery?.data || [];
  const { mutateAsync: deleteVariantAsync } = useDeleteVariant();

  return (
    <div className={cn(className)}>
      <Table>
        <TableCaption>A list of Variants</TableCaption>
        <TableHeader>
          <TableRow>
            <TableHead>Year</TableHead>
            <TableHead>Make</TableHead>
            <TableHead>Model</TableHead>
            <TableHead>Fuel type</TableHead>
            <TableHead>Transmission</TableHead>
            <TableHead>Actions</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          {variants.map((v) => (
            <TableRow key={v.id}>
              <TableCell>{v.year}</TableCell>
              <TableCell>{v.make}</TableCell>
              <TableCell>{v.model}</TableCell>
              <TableCell>{v.fuelType}</TableCell>
              <TableCell>{v.transmission}</TableCell>
              <TableCell>
                <ViewVariantDialog id={v.id} />
                <EditVariantDialog id={v.id} />
                <Button variant="secondary" onClick={() => deleteVariantAsync({ id: v.id })}>
                  <Trash />
                </Button>
              </TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
    </div>
  );
};

export default VariantListTable;
```

```tsx
// automotive.marketplace.client/src/features/variantList/components/CreateVariantDialog.tsx
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { useState } from "react";
import { useCreateVariant } from "../api/useCreateVariant";
import { VariantFormData } from "../types/VariantFormData";
import VariantForm from "./VariantForm";

const CreateVariantDialog = () => {
  const [open, setOpen] = useState<boolean>();
  const { mutateAsync: createVariantAsync } = useCreateVariant();

  const handleSubmit = async (formData: VariantFormData) => {
    await createVariantAsync(formData);
    setOpen(false);
  };

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild>
        <Button>Add variant</Button>
      </DialogTrigger>
      <DialogContent>
        <DialogHeader><DialogTitle>Create new variant</DialogTitle></DialogHeader>
        <VariantForm
          variant={{ year: 0, transmissionId: "", fuelId: "", bodyTypeId: "", doorCount: 0, powerKw: 0, engineSizeMl: 0, isCustom: false, makeId: "", modelId: "" }}
          onSubmit={handleSubmit}
        />
      </DialogContent>
    </Dialog>
  );
};

export default CreateVariantDialog;
```

```tsx
// automotive.marketplace.client/src/features/variantList/components/EditVariantDialog.tsx
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Pencil } from "lucide-react";
import { useState } from "react";
import { useUpdateVariant } from "../api/useUpdateVariant";
import { VariantFormData } from "../types/VariantFormData";
import EditVariantDialogContent from "./EditVariantDialogContent";

type EditVariantDialogProps = { id: string };

const EditVariantDialog = ({ id }: EditVariantDialogProps) => {
  const [open, setOpen] = useState(false);
  const { mutateAsync: updateVariantAsync } = useUpdateVariant();

  const handleSubmit = async (formData: VariantFormData) => {
    await updateVariantAsync({ ...formData, id });
    setOpen(false);
  };

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild><Button variant="secondary"><Pencil /></Button></DialogTrigger>
      <DialogContent>
        <DialogHeader><DialogTitle>Edit variant</DialogTitle></DialogHeader>
        <EditVariantDialogContent id={id} onSubmit={handleSubmit} />
      </DialogContent>
    </Dialog>
  );
};

export default EditVariantDialog;
```

```tsx
// automotive.marketplace.client/src/features/variantList/components/EditVariantDialogContent.tsx
import { useSuspenseQuery } from "@tanstack/react-query";
import { getVariantByIdOptions } from "../api/getVariantByIdOptions";
import { VariantFormData } from "../types/VariantFormData";
import VariantForm from "./VariantForm";

type Props = { id: string; onSubmit: (formData: VariantFormData) => Promise<void> };

const EditVariantDialogContent = ({ id, onSubmit }: Props) => {
  const { data: variantQuery } = useSuspenseQuery(getVariantByIdOptions({ id }));
  const v = variantQuery.data;
  return <VariantForm variant={{ year: v.year, fuelId: v.fuelId, transmissionId: v.transmissionId, bodyTypeId: v.bodyTypeId, doorCount: v.doorCount, powerKw: v.powerKw, engineSizeMl: v.engineSizeMl, isCustom: v.isCustom, makeId: v.makeId, modelId: v.modelId }} onSubmit={onSubmit} />;
};

export default EditVariantDialogContent;
```

```tsx
// automotive.marketplace.client/src/features/variantList/components/ViewVariantDialog.tsx
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Eye } from "lucide-react";
import ViewVariantDialogContent from "./ViewVariantDialogContent";

type Props = { id: string };

const ViewVariantDialog = ({ id }: Props) => (
  <Dialog>
    <DialogTrigger asChild><Button variant="secondary"><Eye /></Button></DialogTrigger>
    <DialogContent>
      <DialogHeader>
        <DialogTitle>Variant details</DialogTitle>
        <DialogDescription>In depth</DialogDescription>
      </DialogHeader>
      <ViewVariantDialogContent id={id} className="grid gap-4" />
    </DialogContent>
  </Dialog>
);

export default ViewVariantDialog;
```

```tsx
// automotive.marketplace.client/src/features/variantList/components/ViewVariantDialogContent.tsx
import { cn } from "@/lib/utils";
import { useSuspenseQuery } from "@tanstack/react-query";
import { getVariantByIdOptions } from "../api/getVariantByIdOptions";

type Props = { id: string; className?: string };

const ViewVariantDialogContent = ({ id, className }: Props) => {
  const { data: variantQuery } = useSuspenseQuery(getVariantByIdOptions({ id }));
  const v = variantQuery.data;

  return (
    <div className={cn(className)}>
      <p>Year: {v?.year}</p>
      <p>Fuel type: {v?.fuel}</p>
      <p>Transmission: {v?.transmission}</p>
      <p>Body type: {v?.bodyType}</p>
      <p>Door count: {v?.doorCount}</p>
      <p>Power: {v?.powerKw} kW</p>
      <p>Engine size: {v?.engineSizeMl} ml</p>
    </div>
  );
};

export default ViewVariantDialogContent;
```

- [ ] **Step 9: Create index.ts**

```typescript
// automotive.marketplace.client/src/features/variantList/index.ts
export { default as VariantListTable } from "./components/VariantListTable";
export { default as CreateVariantDialog } from "./components/CreateVariantDialog";
```

---

## Task 17: Frontend — Variants Page + Route, Delete Cars Page

**Files:**
- Create: `automotive.marketplace.client/src/app/pages/Variants.tsx`
- Create: `automotive.marketplace.client/src/app/routes/variants.tsx`
- Delete: `automotive.marketplace.client/src/app/pages/Cars.tsx`
- Delete: `automotive.marketplace.client/src/app/routes/cars.tsx`
- Modify: `automotive.marketplace.client/src/constants/permissions.ts`

- [ ] **Step 1: Delete Cars page + route**

```bash
rm automotive.marketplace.client/src/app/pages/Cars.tsx
rm automotive.marketplace.client/src/app/routes/cars.tsx
```

- [ ] **Step 2: Create Variants.tsx**

```tsx
// automotive.marketplace.client/src/app/pages/Variants.tsx
import { CreateVariantDialog, VariantListTable } from "@/features/variantList";

const Variants = () => {
  return (
    <div className="grid items-center justify-center space-y-5 pt-10">
      <div className="flex w-full justify-end">
        <CreateVariantDialog />
      </div>
      <VariantListTable className="max-w-3xl" />
    </div>
  );
};

export default Variants;
```

- [ ] **Step 3: Create variants.tsx route**

```tsx
// automotive.marketplace.client/src/app/routes/variants.tsx
import { createFileRoute } from "@tanstack/react-router";
import Variants from "../pages/Variants";

export const Route = createFileRoute("/variants")({
  component: Variants,
});
```

- [ ] **Step 4: Update permissions.ts**

```typescript
// automotive.marketplace.client/src/constants/permissions.ts
export const PERMISSIONS = {
  CreateListings: "CreateListings",
  ManageListings: "ManageListings",
  ViewListings: "ViewListings",
  ViewModels: "ViewModels",
  CreateModels: "CreateModels",
  ManageModels: "ManageModels",
  ViewVariants: "ViewVariants",
  CreateVariants: "CreateVariants",
  ManageVariants: "ManageVariants",
} as const;
```

- [ ] **Step 5: Update Header/nav to link to /variants instead of /cars**

Open `automotive.marketplace.client/src/components/layout/header/Header.tsx` and update any `/cars` links to `/variants`.

- [ ] **Step 6: Re-generate TanStack Router route tree**

TanStack Router auto-generates `routeTree.gen.ts` when files are added/removed in the routes folder. Run the dev server or the route-generation command to regenerate it (exact command may vary — check `package.json` scripts):

```bash
cd automotive.marketplace.client && npm run dev
```

Then verify `routeTree.gen.ts` no longer contains `/cars` and contains `/variants`.

---

## Task 18: Frontend — VariantSelect Component + Update CreateListingForm

**Files:**
- Create: `automotive.marketplace.client/src/components/forms/select/VariantSelect.tsx`
- Modify: `automotive.marketplace.client/src/features/createListing/components/CreateListingForm.tsx`
- Modify: `automotive.marketplace.client/src/features/createListing/schemas/createListingSchema.ts`
- Modify: `automotive.marketplace.client/src/features/createListing/types/CreateListingCommand.ts`
- Modify: `automotive.marketplace.client/src/features/createListing/types/CreateListingFormData.ts`
- Modify: `automotive.marketplace.client/src/features/listingList/types/GetAllListingsResponse.ts`
- Modify: `automotive.marketplace.client/src/features/listingList/components/ListingCard.tsx`

- [ ] **Step 1: Create VariantSelect.tsx**

Fetches non-custom variants for a given modelId (and optional year). The user picks a variant from the dropdown or sees "No matching variant found" to trigger manual entry.

```tsx
// automotive.marketplace.client/src/components/forms/select/VariantSelect.tsx
import { getVariantsByModelOptions } from "@/features/variantList/api/getVariantsByModelOptions";
import { useQuery } from "@tanstack/react-query";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";

type VariantSelectProps = {
  modelId: string;
  year?: number;
  value?: string;
  onValueChange?: (value: string) => void;
  disabled?: boolean;
};

const VariantSelect = ({ modelId, year, value, onValueChange, disabled }: VariantSelectProps) => {
  const { data: variantsQuery } = useQuery(getVariantsByModelOptions({ modelId, year }));
  const variants = variantsQuery?.data || [];

  return (
    <Select value={value} onValueChange={onValueChange} disabled={disabled || !modelId}>
      <SelectTrigger>
        <SelectValue placeholder="Select variant (optional)" />
      </SelectTrigger>
      <SelectContent>
        {variants.map((v) => (
          <SelectItem key={v.id} value={v.id}>
            {v.year} — {v.fuelType} {v.transmission} {v.powerKw}kW {v.engineSizeMl}ml
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  );
};

export default VariantSelect;
```

- [ ] **Step 2: Update CreateListingCommand.ts**

```typescript
// automotive.marketplace.client/src/features/createListing/types/CreateListingCommand.ts
export type CreateListingCommand = {
  variantId?: string;
  modelId?: string;
  fuelId?: string;
  transmissionId?: string;
  bodyTypeId?: string;
  doorCount?: number;
  powerKw?: number;
  engineSizeMl?: number;
  year?: number;
  isCustom?: boolean;
  drivetrainId: string;
  price: number;
  description?: string;
  colour?: string;
  vin?: string;
  mileage: number;
  isSteeringWheelRight: boolean;
  city: string;
  isUsed: boolean;
  images: Blob[];
};
```

- [ ] **Step 3: Update createListingSchema.ts**

```typescript
// automotive.marketplace.client/src/features/createListing/schemas/createListingSchema.ts
import { VALIDATION } from "@/constants/validation";
import { validation } from "@/utils/validation";
import { z } from "zod";

export const CreateListingSchema = z
  .object({
    variantId: z.string().regex(VALIDATION.GUID.REGEX).optional(),
    modelId: z.string().optional(),
    fuelId: z.string().optional(),
    transmissionId: z.string().optional(),
    bodyTypeId: z.string().optional(),
    doorCount: z.coerce.number().int().optional(),
    powerKw: z.coerce.number().int().optional(),
    engineSizeMl: z.coerce.number().int().optional(),
    year: z.coerce.number().int().optional(),
    isCustom: z.boolean().optional(),
    drivetrainId: z
      .string()
      .nonempty({ error: "Please select a drivetrain" }),
    price: z.coerce
      .number<number>()
      .min(VALIDATION.PRICE.MIN, {
        error: validation.minSize({ label: "Price", size: VALIDATION.PRICE.MIN, unit: "€" }),
      })
      .max(VALIDATION.PRICE.MAX, {
        error: validation.maxSize({ label: "Price", size: VALIDATION.PRICE.MAX, unit: "€" }),
      }),
    description: z.string().max(VALIDATION.TEXT.LONG, {
      error: validation.maxLength({ label: "Description", length: VALIDATION.TEXT.LONG }),
    }).optional(),
    colour: z.string().optional(),
    vin: z
      .string()
      .optional()
      .refine((val) => !val || VALIDATION.VIN.REGEX.test(val), {
        message: "VIN must be 17 chars, no I/O/Q",
      }),
    mileage: z.coerce
      .number<number>()
      .int()
      .min(VALIDATION.MILEAGE.MIN, {
        error: validation.minSize({ label: "Mileage", size: VALIDATION.MILEAGE.MIN, unit: "km" }),
      })
      .max(VALIDATION.MILEAGE.MAX, {
        error: validation.maxSize({ label: "Mileage", size: VALIDATION.MILEAGE.MAX, unit: "km" }),
      }),
    isSteeringWheelRight: z.boolean(),
    city: z.string().nonempty({ error: "City cannot be empty" }).max(VALIDATION.NAME.LONG, {
      error: validation.maxLength({ label: "City", length: VALIDATION.NAME.LONG }),
    }),
    isUsed: z.boolean(),
    images: z
      .array(z.instanceof(Blob, { error: "You did not upload a valid image file" }))
      .min(1, { error: "You must upload at least one image" }),
  })
  .superRefine((data, ctx) => {
    if (!data.variantId) {
      if (!data.modelId || !VALIDATION.GUID.REGEX.test(data.modelId))
        ctx.addIssue({ code: z.ZodIssueCode.custom, path: ["modelId"], message: "Please select a model" });
      if (!data.fuelId || !VALIDATION.GUID.REGEX.test(data.fuelId))
        ctx.addIssue({ code: z.ZodIssueCode.custom, path: ["fuelId"], message: "Please select a fuel type" });
      if (!data.transmissionId || !VALIDATION.GUID.REGEX.test(data.transmissionId))
        ctx.addIssue({ code: z.ZodIssueCode.custom, path: ["transmissionId"], message: "Please select a transmission" });
      if (!data.bodyTypeId || !VALIDATION.GUID.REGEX.test(data.bodyTypeId))
        ctx.addIssue({ code: z.ZodIssueCode.custom, path: ["bodyTypeId"], message: "Please select a body type" });
      if (!data.doorCount || data.doorCount < VALIDATION.DOOR_COUNT.MIN)
        ctx.addIssue({ code: z.ZodIssueCode.custom, path: ["doorCount"], message: "Enter door count" });
      if (!data.powerKw || data.powerKw < VALIDATION.POWER.MIN)
        ctx.addIssue({ code: z.ZodIssueCode.custom, path: ["powerKw"], message: "Enter engine power" });
      if (!data.engineSizeMl || data.engineSizeMl < VALIDATION.ENGINE_SIZE.MIN)
        ctx.addIssue({ code: z.ZodIssueCode.custom, path: ["engineSizeMl"], message: "Enter engine size" });
      if (!data.year || data.year < VALIDATION.YEAR.MIN)
        ctx.addIssue({ code: z.ZodIssueCode.custom, path: ["year"], message: "Enter year" });
    }
  });
```

- [ ] **Step 4: Update CreateListingFormData.ts**

```typescript
// automotive.marketplace.client/src/features/createListing/types/CreateListingFormData.ts
import z from "zod";
import { CreateListingSchema } from "../schemas/createListingSchema";

export type CreateListingFormData = z.infer<typeof CreateListingSchema>;
```

- [ ] **Step 5: Update CreateListingForm.tsx**

The form now has a three-step flow:
1. Make → Model → Year (always visible)
2. Variant dropdown (filtered by modelId + year). If a variant is selected → hide spec fields.
3. If no variant selected (or not available) → show full spec fields (fuelId, transmissionId, bodyTypeId, doorCount, powerKw, engineSizeMl) + isCustom checkbox.
4. Drivetrain selector (always visible, replaces old Drivetrain toggle at listing level).
5. Remove `makeId` from what's sent to the API (not needed — variant has the model relationship).

```tsx
// automotive.marketplace.client/src/features/createListing/components/CreateListingForm.tsx
import BodyTypeSelect from "@/components/forms/select/BodyTypeSelect";
import FuelSelect from "@/components/forms/select/FuelSelect";
import MakeSelect from "@/components/forms/select/MakeSelect";
import ModelSelect from "@/components/forms/select/ModelSelect";
import VariantSelect from "@/components/forms/select/VariantSelect";
import DrivetrainToggleGroup from "@/components/forms/DrivetrainToggleGroup";
import TransmissionToggleGroup from "@/components/forms/TransmissionToggleGroup";
import { Button } from "@/components/ui/button";
import { Checkbox } from "@/components/ui/checkbox";
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from "@/components/ui/form";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { zodResolver } from "@hookform/resolvers/zod";
import { useForm } from "react-hook-form";
import { useCreateListing } from "../api/useCreateListing";
import { CreateListingSchema } from "../schemas/createListingSchema";
import { CreateListingFormData } from "../types/CreateListingFormData";
import ImageUploadInput from "./ImageUploadInput";

type CreateListingFormProps = { className?: string };

const CreateListingForm = ({ className }: CreateListingFormProps) => {
  const form = useForm<CreateListingFormData>({
    resolver: zodResolver(CreateListingSchema),
    defaultValues: {
      variantId: undefined,
      modelId: "",
      fuelId: "",
      transmissionId: "",
      bodyTypeId: "",
      doorCount: 0,
      powerKw: 0,
      engineSizeMl: 0,
      year: undefined,
      isCustom: false,
      drivetrainId: "",
      price: 0,
      vin: "",
      mileage: 0,
      isSteeringWheelRight: true,
      city: "",
      colour: "",
      isUsed: false,
      images: [],
    },
  });

  const { mutateAsync: createListingAsync } = useCreateListing();

  const onSubmit = async (formData: CreateListingFormData) => {
    await createListingAsync(formData);
    form.reset();
  };

  const selectedMake = form.watch("makeId");
  const selectedModel = form.watch("modelId");
  const selectedYear = form.watch("year");
  const selectedVariantId = form.watch("variantId");
  const showSpecFields = !selectedVariantId;

  return (
    <div className={className}>
      <Form {...form}>
        <form
          onSubmit={form.handleSubmit(onSubmit)}
          className="grid w-full min-w-3xs grid-cols-2 gap-x-6 gap-y-6 md:grid-cols-6 md:gap-x-12 md:gap-y-12"
        >
          {/* Make / Model / Year */}
          <FormField name="makeId" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Car make*</FormLabel>
              <FormControl>
                <MakeSelect isAllMakesEnabled={false} onValueChange={field.onChange} {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="modelId" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Car model*</FormLabel>
              <FormControl>
                <ModelSelect isAllModelsEnabled={false} disabled={!selectedMake} onValueChange={field.onChange} selectedMake={selectedMake} {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="year" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Year</FormLabel>
              <FormControl><Input type="number" min={0} {...field} /></FormControl>
              <FormMessage />
            </FormItem>
          )} />

          {/* Variant selector */}
          <FormField name="variantId" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 md:col-span-4 flex flex-col justify-start">
              <FormLabel>Select matching variant (optional)</FormLabel>
              <FormControl>
                <VariantSelect
                  modelId={selectedModel}
                  year={selectedYear}
                  value={field.value}
                  onValueChange={field.onChange}
                  disabled={!selectedModel}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )} />

          {/* Spec fields (shown only when no variant selected) */}
          {showSpecFields && (
            <>
              <FormField name="transmissionId" control={form.control} render={({ field }) => (
                <FormItem className="col-span-2 flex flex-col justify-start">
                  <FormLabel>Transmission type*</FormLabel>
                  <FormControl>
                    <TransmissionToggleGroup type="single" onValueChange={field.onChange} {...field} />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )} />
              <FormField name="fuelId" control={form.control} render={({ field }) => (
                <FormItem className="col-span-1 flex flex-col justify-start">
                  <FormLabel>Fuel type*</FormLabel>
                  <FormControl><FuelSelect onValueChange={field.onChange} {...field} /></FormControl>
                  <FormMessage />
                </FormItem>
              )} />
              <FormField name="bodyTypeId" control={form.control} render={({ field }) => (
                <FormItem className="col-span-1 flex flex-col justify-start">
                  <FormLabel>Body type*</FormLabel>
                  <FormControl><BodyTypeSelect onValueChange={field.onChange} {...field} /></FormControl>
                  <FormMessage />
                </FormItem>
              )} />
              <FormField name="doorCount" control={form.control} render={({ field }) => (
                <FormItem className="col-span-2 flex flex-col justify-start">
                  <FormLabel>Door count*</FormLabel>
                  <FormControl><Input type="number" {...field} /></FormControl>
                  <FormMessage />
                </FormItem>
              )} />
              <FormField name="powerKw" control={form.control} render={({ field }) => (
                <FormItem className="col-span-2 flex flex-col justify-start">
                  <FormLabel>Engine power (kW)*</FormLabel>
                  <FormControl><Input type="number" min={0} {...field} /></FormControl>
                  <FormMessage />
                </FormItem>
              )} />
              <FormField name="engineSizeMl" control={form.control} render={({ field }) => (
                <FormItem className="col-span-2 flex flex-col justify-start">
                  <FormLabel>Engine size (ml)*</FormLabel>
                  <FormControl><Input type="number" min={0} {...field} /></FormControl>
                  <FormMessage />
                </FormItem>
              )} />
              <FormField name="isCustom" control={form.control} render={({ field }) => (
                <FormItem className="col-span-1 flex flex-col items-center justify-start">
                  <FormLabel>Custom/modified variant</FormLabel>
                  <FormControl>
                    <Checkbox checked={field.value} onCheckedChange={field.onChange} className="h-5 w-5" />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )} />
            </>
          )}

          {/* Listing-level fields */}
          <FormField name="drivetrainId" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Drivetrain*</FormLabel>
              <FormControl>
                <DrivetrainToggleGroup type="single" onValueChange={field.onChange} {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="price" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Car price (€)*</FormLabel>
              <FormControl><Input type="number" min={0} {...field} /></FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="description" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start md:col-span-4">
              <FormLabel>Description</FormLabel>
              <FormControl><Textarea className="max-h-96" {...field} /></FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="images" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Vehicle images*</FormLabel>
              <FormControl><ImageUploadInput field={field} /></FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="city" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>City*</FormLabel>
              <FormControl><Input placeholder="Kaunas" type="text" {...field} /></FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="mileage" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Mileage (km)*</FormLabel>
              <FormControl><Input type="number" min={0} {...field} /></FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="vin" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>VIN</FormLabel>
              <FormControl><Input placeholder="1G1JC524417418958" type="text" {...field} /></FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="colour" control={form.control} render={({ field }) => (
            <FormItem className="col-span-2 flex flex-col justify-start">
              <FormLabel>Colour</FormLabel>
              <FormControl><Input placeholder="Crimson" type="text" {...field} /></FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="isSteeringWheelRight" control={form.control} render={({ field }) => (
            <FormItem className="col-span-1 flex flex-col items-center justify-start">
              <FormLabel>Steering wheel on right</FormLabel>
              <FormControl>
                <Checkbox checked={field.value} onCheckedChange={field.onChange} className="h-5 w-5" />
              </FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <FormField name="isUsed" control={form.control} render={({ field }) => (
            <FormItem className="col-span-1 flex flex-col items-center justify-evenly">
              <FormLabel>Used car</FormLabel>
              <FormControl>
                <Checkbox checked={field.value} onCheckedChange={field.onChange} className="h-5 w-5" />
              </FormControl>
              <FormMessage />
            </FormItem>
          )} />
          <Button className="col-span-2 md:col-start-3" type="submit">Submit</Button>
        </form>
      </Form>
    </div>
  );
};

export default CreateListingForm;
```

- [ ] **Step 6: Update GetAllListingsResponse.ts**

Rename `power` → `powerKw`, `engineSize` → `engineSizeMl`.

```typescript
// automotive.marketplace.client/src/features/listingList/types/GetAllListingsResponse.ts
export type GetAllListingsResponse = {
  id: string;
  isUsed: boolean;
  year: number;
  make: string;
  model: string;
  mileage: number;
  price: number;
  engineSizeMl: number;
  powerKw: number;
  fuelType: string;
  transmission: string;
  city: string;
  description: string;
  images: Image[];
};

type Image = {
  url: string;
  altText: string;
};
```

- [ ] **Step 7: Update ListingCard.tsx**

Update `listing.engineSize` → `listing.engineSizeMl` and `listing.power` → `listing.powerKw`:

```tsx
// automotive.marketplace.client/src/features/listingList/components/ListingCard.tsx
// (only the changed stat line — rest of the component stays the same)
stat={`${listing.engineSizeMl / 1000} l ${listing.powerKw} kW`}
```

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat: frontend — variantList feature, Variants page, updated CreateListingForm

- Replace carList feature folder with variantList (all CRUD dialogs, types, schemas, API hooks)
- Add VariantSelect component for Make→Model→Year→Variant dropdown flow in CreateListingForm
- CreateListingForm: find-or-create flow (variantId path + full spec path + isCustom)
- DrivetrainId replaces Drivetrain enum at listing level
- Enum types include id field; toggle/select components use id as form value
- GetAllListingsResponse: power→powerKw, engineSize→engineSizeMl
- Permission constants: ViewCars→ViewVariants, CreateCars→CreateVariants, ManageCars→ManageVariants
- Add /variants route; remove /cars route

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
```

---

